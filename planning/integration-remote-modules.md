# 원격 모듈 메인 연결 단위

2026-09-21 pc1. 시작 main cc6c6d1. 결과 단위: 상담원이 새 CallReview에서 통화를 끝까지 재생한 뒤 정제하고, 현재 입력/확인 상태를 대조하며 같은 접수의 새 WMS/TMS 화면에서 근거를 저장하고 센터 흐름으로 돌아옵니다.

## 입력·소유·계약

- pc2 ef8f33e(컴포넌트는7ae648e와 동일), pc3 d6aa6b3, pc4 1b82f1d를 고정 입력으로 사용합니다. 원격 소유 컴포넌트/스타일 원문만 명시 경로로 가져오며 후속 미디어 제작 브랜치의 미완성 변경을 자동 합치지 않습니다.
- pc1 소유 page.tsx의 부모 연결, 상태/오류/분석 게이트, 통합 검사만 변경합니다. 기존 API/fixture/정본 미디어와 상태 원장은 유지합니다.
- CallReview는 원문/전사·AI제안·현재입력을 표시하고 분석 API는 부모가 호출합니다. `intake: form`과 `reviewConfirmed: confirmed`를 함께 전달합니다. 분석 오류/재시도는 저장 오류와 분리합니다.
- 음성 전체 재생 완료는 case/channel/audioUrl별로 보관합니다. 새 사건/새 음원은 미완료이며 seek-ended/발화구간/disabled/이전사건 이벤트를 통과로 세지 않습니다. replay/demo-live 모두 음성 종료 후 수동 실행, 텍스트는 STT 재생 조건 없음입니다.
- 선택 모드와 이미 표시하는 결과의 출처를 구분합니다. 저장된 결과가 있는 모드만 그 결과의 출처로 표시하고, 분석 전 합성 fixture 대화록은 시연 원문입니다.
- WmsScene/TmsScene은 `caseData/onBack/onLinkEvidence`를 받습니다. 저장 콜백은 실제 PATCH 성공 후에만 resolve하고 실패는 reject합니다. current revision을 사용하며 새 접수·오프라인·다른 사건의 선택을 성공처럼 보이게 하지 않습니다.

## 작은 Bolt와 검증

병목은 원격 모듈이 독립 harness에서만 동작하는 상태입니다. 실제 셸에 연결한 뒤 통화→정제→수정/확인→근거 저장→센터→경영주를 실행합니다. 한 변경은 부모 연결이며 신규 음성/영상·분석 모델 변경을 섞지 않습니다.

1. 컴포넌트 원문 SHA 대조, TypeScript와 production build.
2. 실제 브라우저에서 전체 음성 자연종료/seek/재시도/사건 전환, 현재 입력 수정 시 확인표시 해제.
3. 두 사례 WMS/TMS 근거 PATCH 성공,503실패에 거짓 성공0, 명시linked INT의 정상근거와 미등록영상 구분.
4. 센터 회신/잔여조치종결 차단·경영주 조회, 원문/사건/정본자산 보존.
5. 3폭 시각·키보드·콘솔 검수와 같은 새빌드6회는 별도 실제 실행 결과로 남깁니다. 이전셸 PASS를 소급하지 않습니다.

pc3 linked INT의 함수/SSR58검사와 pc2 실제 컴포넌트 동작 검사는 선행 증거이며 최종 통합 통과가 아닙니다. pc4의 계획시간을 실적미채택이라 부르는 문구와 비문의 방문의 명시적 이질관계 경고는 독립검토의 P2 잔여 후보입니다. 실제 PATCH 혼입·새 P0/P1이 생기면 통합 인수를 중단하고 재현·수정합니다.
