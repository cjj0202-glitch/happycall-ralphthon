# 작업 폴더와 커밋 기준

이 프로젝트의 작업 폴더는 `C:/00.프로젝트/happycall-ralphthon`입니다. 상위 `C:/00.프로젝트`나 사내 운영플랫폼 폴더를 제품 저장소로 사용하지 않습니다. 여러 로컬 보조 작업은 **같은 작업 폴더를 공유**하며 별도 Git worktree가 아닙니다. 소유 파일을 나누고 기존 변경을 보존합니다. 다른 물리 PC는 각자 등록한 저장소 경로를 사용합니다.

원격 결과를 읽기 전용으로 검토하는 예외 사본은 `.local/review-pc3-577a31b`입니다. pc3 결과 `577a31b`의 detached Git worktree로, 메인 작업 폴더나 운영 런타임을 바꾸지 않습니다. 제품 편집은 여전히 위 메인 경로의 소유권을 따르며 검토 사본을 최신 배포로 혼동하지 않습니다. 원본 보고·실패 이력과 함께 보존합니다.

후속 관측: 이 pc3 검토 사본은 수정 결과 `d6aa6b3`로 갱신했습니다(폴더 이름은 처음 검토한 SHA). pc2 결과 `7ae648e`는 `.local/review-pc2-7ae648e`의 별도 detached worktree에서 읽습니다. 두 사본은 제품 런타임이 아닙니다. `.local/tools/blender`는 공식 portable 도구, `.local/media-benchmark`는 렌더 실측용이며 Git에 넣지 않습니다.

pc4 결과 `1b82f1d`의 읽기 전용 검토 사본은 `.local/review-pc4-1b82f1d`입니다. `.local/layout-study`는 원천 도면 렌더·추출·해시·내부 대조 기록으로 Git 제외이며, 외부 공유는 독립 합성 설계만 사용합니다.

| 위치 | 보관하는 내용 | 공유 기준 |
|---|---|---|
| `apps/web/` | Next 화면·스타일·클라이언트 코드 | 검증한 소스·lock 파일 |
| `server/` | API·업무 규칙·저장·배포 진입점 | 검증한 소스 |
| `data/fixtures/` | 독립 합성 사례 | 원천 업무 행·식별값 제외 |
| `planning/`, `docs/` | 결정·계약·설계·작업 지시 | 최신 적용 범위와 과거 기록 구분 |
| `tests/`, `reports/` | 재현 검사·정제된 검증 결과 | 비밀값·실제 운영 내용 제외 |
| `scripts/`, `ops/`, `channel/` | 실행·작업표·PC 협업 도구 | 실행기와 계약, 로컬 상태 제외 |
| `.local/` | 접수 상태·예산 원장·임시 검사·원천 구조 상세 | Git 제외. 초기화하거나 임의 삭제하지 않음 |
| `.venv/`, `node_modules/`, `.next*/`, `out/`, `dist/` | 의존성·개발/빌드 결과 | Git 제외 |
| `apps/web/public/demo/` | 실행할 합성 음성·영상 | Git 제외. 검증한 Release 자산으로 전달 |
| OneDrive `_PC간채널/04_첨부/…` | 30/33/36 원천 자료 | 원래 위치에서 로컬 참조. 저장소로 복사·업로드하지 않음 |

키는 로컬 비밀 설정이나 배포 서버 비밀값으로 관리합니다. `.env*` 실제 값·인증정보·원본 대화 로그는 Git에 넣지 않습니다. 이 경로 지도는 실제 실행 위치를 대신하지 않으므로 시작할 때 `python channel/whoami.py`와 `git status --short`를 확인합니다.

## 중간 저장과 정리

사용자 지시에 따라 설계 확정·구현 검증·독립 검토·통합 완료 등 복구 가능한 단위로 커밋하고 공유합니다. 한 커밋에 기능·재현 테스트·해당 증거를 묶되 작업 중인 다른 담당자의 파일은 섞지 않습니다. 명시 경로로 add/commit하고 커밋의 파일 목록과 원격 HEAD를 대조합니다. push는 다른 PC의 수신 완료를 의미하지 않습니다.

폴더 이동이 필요하면 참조와 실행 경로부터 검사합니다. 기존 보고서·실패 증거·원장·사용자 변경을 정리 명목으로 지우지 않습니다. 파일을 새로 만들 때 위 목적에 맞는 경로를 사용하고, 중복 안내는 복사보다 정본 링크를 사용합니다.
