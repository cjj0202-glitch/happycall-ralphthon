# 팀원에게 그대로 보내는 글

> 메인이 카톡·슬랙에 붙여넣어 보냅니다. 보내기 전에 **`<>` 안을 채우십시오.**
> `<레포주소>` 는 한 번만, `<슬롯>`·`<역할>`·`<첫 덩어리>` 는 **사람마다 다릅니다.**

---

## 📋 여기서부터 복사

```
해커톤 준비입니다. 노트북에서 10분이면 끝납니다.

■ 1단계 — 저한테 먼저 알려주세요 (이것부터)

터미널(PowerShell)을 열고:

    gh auth login          (이미 했으면 건너뛰세요)
    gh api user --jq .login

마지막 줄에 나온 이름을 저한테 보내주세요. 그걸로 저장소에 초대합니다.
gh 가 없으면: winget install --id GitHub.cli

■ 2단계 — 초대 수락

제가 초대하면 메일이 오거나 https://github.com/notifications 에 뜹니다. 수락해주세요.

수락됐는지 확인:

    gh api repos/<레포주소> --jq .full_name

저장소 이름이 나오면 OK. 404 가 나오면 아직 수락이 안 된 겁니다.
🚨 이걸 건너뛰면 나중에 원인 못 찾고 시간만 씁니다.

■ 3단계 — 받기

    git clone https://github.com/<레포주소>
    cd happycall-ralphthon
    python channel/whoami.py

여기서 「미등록」이 나오는 게 정상입니다.
출력에 있는 hostname 한 줄을 저한테 보내주세요. 제가 등록하고 알려드립니다.

🚨 파일을 직접 고치지 마세요. 제가 등록해야 편지가 제대로 갑니다.

■ 4단계 — 제 연락 받고

    git pull
    python channel/whoami.py

슬롯이 제대로 나오면 준비 끝입니다.

■ 5단계 — 도구 확인

    python --version      (3.12 이상)
    node --version
    codex --version

codex 가 없으면: npm install -g @openai/codex  후  codex login
설치했는데 「Missing optional dependency」가 뜨면 아래로 다시 깔아주세요.
(와이파이가 끊기면 조용히 반쪽만 설치됩니다)

    npm uninstall -g @openai/codex
    npm install -g @openai/codex

■ 6단계 — 시작

터미널 2개를 엽니다.

  [터미널 A] 알림용 — 켜두기만 하면 됩니다
    python channel/mail.py watch --interval 30 --bell

  [터미널 B] 작업용 — 여기서 codex 를 띄우고 제가 따로 보낼 프롬프트를 붙여넣으세요

──────────────────────────────────────────────
막히면 그냥 저한테 물어보세요. 혼자 30분 이상 붙잡지 마세요.
```

## 📋 여기까지

---

# 그 다음 — 사람마다 따로 보내는 프롬프트

슬롯을 등록해 준 뒤, **그 사람 몫만** 채워서 보냅니다.
아래는 `docs/01_ChatGPT_전달_프롬프트.md` §1 과 같은 내용입니다.

```
너는 이 레포에서 일하는 에이전트다. 먼저 AGENTS.md 를 읽고 그대로 따르라.

## 신원
너는 <슬롯> 이고 역할은 <역할> 이다.
시작하기 전에 `python channel/whoami.py` 를 돌려라.
슬롯이 <슬롯> 이 아니거나 「미등록」이면 거기서 멈추고 그 출력만 나에게 보고하라.
직접 pcs.json 을 고치지 마라.

## 지금 맡은 것
<첫 덩어리 — 한 덩어리만>

## 반복할 것
1. `python channel/mail.py watch --once` 를 부른다.
   조용하면 새것이 없는 것이다. 그냥 3 으로 간다.
   🚨 터미널 감시기(--interval 30)는 사람이 보는 것이고 너를 깨우지 못한다.
   너는 루프 맨 위에서 --once 를 «직접» 불러야 한다.
2. 출력이 있었으면 `python channel/mail.py inbox` 로 본다.
   - 편지가 있으면 `read` 하고, 6칸 중 「검증 방법 + 기대값」을 먼저 읽는다.
   - 기대값을 못 맞추면 done 하지 마라. `reply` 로 막힌 지점을 수치와 함께 적어라.
   - 다 됐으면 `python channel/mail.py done <번호> --evidence "<무엇을 어떻게 확인했는가>"`
3. 맡은 덩어리를 끝낸다.
4. `python channel/mail.py status --set "<지금 하는 일 한 줄>"` 로 상태를 갱신한다.
5. 1 로 돌아간다.

## 규칙 — 어기면 멈춘다
- 상태 보고를 편지(이슈)로 만들지 마라. `status --set` 만 쓴다.
- 다른 워커에게 직접 지시하지 마라. 필요하면 pc1(메인)에게 유형 C 로 요청한다.
- 「에러 없음」을 검증이라고 쓰지 마라. 수치와 시각을 적어라.
- 인용할 때 「그쪽」이라고 쓰지 마라. 슬롯명(pc2 등)으로 적어라.
- 시크릿 값을 편지에 넣지 마라. 경로만 적어라.
- inbox 를 무조건 부르지 마라. watch --once 가 조용하면 부를 이유가 없다.
- 남의 앞으로 온 편지를 대신 처리하거나 옮기지 마라. 중계가 편지를 4배로 만든다.
- 영상·음성 파일을 git 에 커밋하지 마라. `gh release upload demo-assets <파일> --clobber` 를 쓴다.
- 한 가지에 30분 막히면 루프를 멈추고 나에게 올려라. 다른 워커에게 넘기지 마라.

## 끝났다고 말할 수 있는 조건
<이게 되면 끝이다 — 구체적인 값으로>
```

---

# 🚨 메인이 보내기 전 확인할 것

| 확인 | 왜 |
|---|---|
| `<레포주소>` 를 `cjj0202-glitch/happycall-ralphthon` 같은 실제 값으로 바꿨는가 | 그대로 보내면 아무도 못 받는다 |
| 초대를 **먼저** 보냈는가 | 수락이 안 되면 3단계 clone 부터 막힌다 |
| `pcs.json` 에 hostname 을 넣고 **push 했는가** | push 안 하면 상대가 `git pull` 해도 여전히 미등록이다 |
| `<첫 덩어리>` 가 **한 덩어리**인가 | 크게 주면 중간에 길을 잃는다 |
| `<끝났다고 말할 수 있는 조건>` 을 채웠는가 | 비우면 랄프 루프가 안 멈춘다 |
