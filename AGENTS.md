# 이 레포에서 일하는 에이전트 상시 지침

**통합 실행 지침:** [PROMPT_team.md](PROMPT_team.md)에 제품 요구·BMAD·Grill-me·First Bolt·Ralph·공식 배점과 증거 프로토콜을 연결했습니다. 워커 시작 시 전체를 읽습니다.

**09시까지 반복 운영:** 사용자가 요청한 [happycall-night-ops](.agents/skills/happycall-night-ops/SKILL.md)를 적용합니다. 기존 배정 회신→한 검증 단위→영향 테스트→명시 경로 커밋·공유→메인 인수를 반복하며, 현재 30분 heartbeat를 중복 생성하지 않습니다. 스킬 설치와 실제 재개·원격 착수는 별도 사실입니다.

> Codex CLI 는 이 파일을 자동으로 읽습니다. Claude Code 도 같은 내용을 `CLAUDE.md` 로 읽습니다.
> **세션을 시작하면 이 파일을 먼저 따르십시오.**

## 0. 첫 30초 — 신원부터

```bash
python channel/whoami.py
```

**슬롯이 「미등록」으로 나오면 편지 처리와 제품 작업을 멈추십시오.** `channel/pcs.json` 의 자기 슬롯
`hostname` 등록은 메인에게 요청하고, 반영된 설정을 받은 뒤 다시 돌립니다. 등록 전에 편지함을 읽으면
**남의 편지를 처리하는 사고**가 납니다. 이건 실제로 났던 사고입니다.

등록 전에도 `scripts/preflight.py`의 읽기 전용 환경 점검과 사전 준비 문서 열람은 가능합니다. 미등록 PENDING을 PASS로 바꾸거나 남의 슬롯을 지정하지 않습니다.

## 1. 이 레포가 무엇인가

Codex도 스킬을 지원합니다. `.agents/skills/mailbox`와 `.agents/skills/ralph-loop`가
`.claude/skills`의 공유 원본을 읽습니다. 규약의 정본은 이 파일과 `channel/00_채널규약.md`입니다.

노트북 4대가 랄프톤(에이전트 루프) 방식으로 **해피콜 시스템**을 만듭니다.
4대는 서로 직접 대화할 수 없으므로 **GitHub Issues 를 편지함으로** 씁니다.

```
받은편지함 = to:<내슬롯> 라벨이 붙은 open 이슈
보내기     = 이슈 생성      회신 = 코멘트      처리완료 = close
상태보고   = 편지가 아니다. 고정 상태 이슈 body 를 덮어쓴다
```

상세는 [channel/00_채널규약.md](channel/00_채널규약.md), 4대 구조는
[docs/00_랄프톤_운영설계.md](docs/00_랄프톤_운영설계.md).

## 2. 매 루프에서 하는 일

**현재 착수 예외와 공유 컨텍스트:** [docs/21_공유_작업컨텍스트.md](docs/21_공유_작업컨텍스트.md)를 먼저 읽습니다. DEC-013에 따라 사용자는 시간 제약으로 로컬 병렬 구현·AI Playwright 검증을 요청했습니다. 아래 기존 4PC/사람 검증 게이트의 완료를 뜻하지 않으며, 등록된 원격 PC의 독립 설계·구현은 DEC-016과 docs/24에 따라 배정합니다. TEST는 별도 미완료로 보존하고 정책 거부를 우회하지 않습니다. 결정·계약 변경은 메인이 공유 문서와 기준 커밋으로 전달합니다.

제품 작업의 기준은 `TODO.md`와 `docs/03_목표와_범위.md`~`docs/06_검증과_시연.md`입니다. 현재 배포 대상은 DEC-021의 **52g Studio / g-28 Vercel**이며 인증·저장소·배포 상태는 `reports/deployment/account-auth-status.md`를 대조합니다. 아래 DEC-019의 로그인 대기 관측은 이전 이력입니다.
새 메인 세션은 `docs/15_새_메인세션_상세_인수인계.md`, 다른 PC 준비는 `docs/14_팀원_PC_채팅용_프롬프트.md`에서 시작합니다. 네 사람 설계회의는 docs/13, 교육 First Bolt 적용과 출처 한계는 docs/16을 읽습니다. 일반 배정은 B01~B03/P03/G02를 따르며, 현재 사용자 즉시구축 요청의 착수 예외는 DEC-013·016과 docs/24가 우선합니다. DEC-018에 따라 9/22 09시 배포까지 재확인 없이 진행합니다. 호스팅·인증의 최신 사실은 DEC-019와 docs/12를 따르며, 로그인 재시도를 최종 배포 대상 확정으로 해석하지 않습니다.
행사 일정·채점·제출은 `docs/09_공식규정_전수대조.md`가 우선입니다. `GOAL.md`의 긴 블록은 참조 초안이며 17:22:02 KST 실제 create_goal 시작 원문은 reports/goal-start-20260921.md입니다. 실제 로그/제출은 별도 확인합니다. 원본 JSONL/ZIP은 GitHub에 올리지 않습니다.
`ops/tasks.json`은 pc1만 변경합니다. 워커는 배정된 DOING 작업 하나를 끝내고 증거·커밋을 회신합니다.
pc1이 REVIEW → 증거 대조 → `ops/tasks.py accept`를 마친 뒤에만 TODO를 체크하고 이슈를 닫습니다.
신규 세션은 자기 열린 편지와 배정 작업을 한 번 확인합니다. 이후 `watch --once`는 변경 감지에 씁니다.
알림이 없다는 사실은 미처리 작업이 없다는 뜻이 아닙니다. 더 배정된 일이 없으면 종료합니다.

별도 터미널에서 감시기를 띄워 둡니다 — **새것이 있을 때만** 울립니다.

```bash
python channel/mail.py watch --interval 30 --bell
```

감시기는 내 앞으로 온 편지와 내가 보낸 편지의 회신·종결을 읽습니다. 시작·오류·종료 안내를
제외하면 변화가 없을 때 출력하지 않습니다. 터미널 알림만으로 Codex 대화가 자동 재개되지는 않습니다.

그리고 작업 터미널에서:

```bash
python channel/mail.py inbox                      # 내 앞 편지
python channel/mail.py read <번호>                 # 본문
# ... 실제 작업 ...
python channel/mail.py status --set "지금 무엇을 하는 중"
python channel/mail.py reply <번호> --body "작업 ID·커밋·증거 경로·기대/실측"
# pc1이 인수 검증하고 tasks.py accept한 뒤에만 mail.py done으로 닫습니다.
```

## 3. 🚨 이것만은

| # | 규칙 | 왜 |
|---|---|---|
| 1 | **상태보고를 편지로 만들지 않는다** | 원본 채널 실측에서 상태보고가 51.5%였다. 편지로 만들면 편지함이 상태로 덮여 진짜 지시가 묻힌다. `mail.py status --set` 를 쓴다 |
| 2 | **감지는 감시기(`watch`)에 맡기고, 루프 안에서 inbox 를 반복 호출하지 않는다** | 「폴링이 편지를 부른다」 — 감시 세션을 켠 날 편지가 6.4통/h → 25.3통/h(4배)로 뛰었다. 터진 것은 «읽는 폴링»이 아니라 폴링 세션이 **중계 허브**가 된 것이었다. `watch` 는 읽기만 하고 편지를 만들지 않으므로 그 부작용이 없다 |
| 3 | **워커끼리 직접 지시하지 않는다** | 지시는 메인(pc1)만 만든다. 워커 간 교차 지시는 누가 무엇을 맡았는지 아무도 모르게 만든다 |
| 4 | **「에러 없음」은 검증이 아니다** | 무엇을 보고 됐다고 판단했는지 수치·시각으로 적는다. 기대값이 없으면 받는 쪽이 자기 자로 재서 통과시킨다 |
| 5 | **「그쪽」이라고 쓰지 않는다** | 한 PC 에 세션이 여럿이다. 합쳐 읽으면 자기가 한 적 없는 판단을 되돌려받는다. 슬롯명으로 인용한다 |
| 6 | **시크릿 값을 편지 본문에 넣지 않는다** | 경로만 보낸다. `mail.py` 가 발송 전에 검사하고 막지만, 검사기를 믿고 붙여넣지 마십시오 |
| 7 | **회신 없이 닫지 않는다** | `mail.py done` 이 `--evidence` 를 요구한다. 「조용하다」가 「안 했다」로 읽힌다 |
| 8 | **막힌 일을 남에게 넘기지 않는다** | 권한 세탁이다. 막혔으면 사람에게 올린다 |

## 4. 합의한 것은 파일로 옮긴다

> 채널은 **전달 수단이지 보관 수단이 아니다.**

규칙·판정 기준·인터페이스 계약을 이슈에서 합의했으면 **그 자리에서** 어느 파일의
몇 절인지 정하고 옮깁니다. 정할 곳이 없으면 그 합의는 내일 없습니다.
PC 별 진행 상황은 옮기지 않습니다 — 낡아서 거짓이 됩니다.

## 5. 산출물

| 무엇 | 어디 | 왜 |
|---|---|---|
| 코드·설계·규칙 | 이 레포 | git 이 나른다 |
| 영상·음성 같은 큰 파일 | **GitHub Release 자산** (`gh release upload`) | 레포에 넣으면 100MB 제한과 clone 시간에 걸린다. `.gitignore` 가 이미 막는다 |
| 시크릿·API 키 | **각 PC 로컬 `.env`** | 레포·이슈 어디에도 올리지 않는다 |

**최신 사용자 승인(DEC-018):** 2026-09-22 09:00 배포까지 기존 과업의 필요한 작업은 사전 승인됐고 재확인하지 않습니다. 과거 보류 문구보다 이 결정이 우선합니다. 실제 계정·프로젝트·인증·배포 URL은 도구로 검증하며 원천자료 비공개·정책차단 비우회 경계는 유지합니다.
