# N02-R2 PC2 독립 반례 — 메인 서버 보완

2026-09-22 · 기준 main `f043c60` · pc1/최제준의 로컬 하위 에이전트 구현

## 문제와 수정 범위

PC2의 고정 보고 `4c200dec13433d7139151a92aac89d8a13a076f4:reports/pc2/multiple-request-implementation-review.md`는 기존 지정 회귀 233개가 통과해도 새 독립 입력 20개 중 8개에서 공개 요청이 잘못 유지되거나 제거됨을 보여 줍니다. 이전 구현 보고와 PC2 원본 보고·기대값은 보존합니다.

| 경계 | PC2 실패 | 보완 |
|---|---|---|
| 같은 상품의 조사 차이 | D01: `치약을`/`치약`을 다른 상품으로 판정 | 공통 비교용 명사 표면형·조사 제거형을 사용하되, 불명확한 주격 `이/가`로 시작하는 말음은 절삭하지 않습니다. `파이`와 `파`는 다르게, `파이를`과 `파이`는 같게 비교합니다. 한 글자 상품의 명시 비교 결과도 유지하며 인용 원문은 바꾸지 않습니다. |
| 취소 대상을 제한하는 조사 | D04/D06: `요청만`에서 대상 분리가 실패 | 대상 비교와 요청 끝 조사에 같은 최대2개 결합 조사 문법을 사용하여 `만을/만은/을만`을 처리합니다. 작업·상품 동의어 목록을 추가하지 않습니다. |
| 인용 안 연속 문장부호 | D08/D09: `?!` 뒤 부정 문맥이 분리 | 연속 `. ! ?` 전체를 하나의 경계로 소비한 뒤 닫는 따옴표와 보고/부정 문맥을 확인합니다. 현재 요청·취소 검사에 같은 문장 끝 함수를 사용합니다. |
| 구형 계약의 익명 화자 | D12/D15: 신형 가드 뒤 legacy 검사에서 동일인 취소로 오인 | 출처 결합·취소·legacy 후속 구간·단위 정정 앞 문맥이 공통 명시 화자 판정을 사용합니다. 익명 라벨 후보 보존은 고객 신원 확정이 아닙니다. |
| 단위 정정 연결어 | D17: `아닌` 정정값을 뺀 단축 요청 활성화 | 기존 `아니라`와 관형 부정 `아닌` 문맥을 동일하게 검토합니다. 원문 자동 확장·수량/단위 추출은 하지 않습니다. |

수정 파일은 `server/request_grounding.py`, `server/request_provenance.py`, `tests/test_multi_request_provenance.py`, 이 보고서입니다. 공개 schema·normalizer·수량·단위 null 정책·예산·FE·기존 보고서는 변경하지 않습니다. 커밋·push·API 호출은 하지 않습니다.

## 수정 전 동일 실행기 재현

PC2 보고의 `r2-verifier` Python 블록을 그대로 추출합니다. 하위 에이전트의 `.venv/Scripts/python.exe -X utf8 -B -`는 base Python 프로세스 생성 실패로 실행되지 않았습니다. 제품 수정 전 독립 검토자가 clean main `f043c60`에서 원 실행기를 실제 실행했습니다.

- **12/20 일치, 8/20 불일치**: `D01,D04,D06,D08,D09,D12,D15,D17`이 PC2와 정확히 같습니다.
- 증거: ignored `.local/main-resume-20260922/pc2-r2-intake/before-main-f043c60.json`.
- 제품 4파일과 R1 JSON은 실행 전후 동일하며 모델·키·예산 원장 경계 호출은 0입니다.
- 실행기 exit 0은 관측 저장 성공입니다. 제품 20개 통과로 표시하지 않습니다.

## 검사와 재현

PC2 D01–D20의 입력·기대 활성·거절·검토 이유를 수정 없이 테스트에 고정했습니다. resolver와 공개 요청 투영·수량/단위 null·입력 불변·실제 활성 출처 span/시각을 대조합니다. 기존 회귀에 문장부호 조합 16개, 구형 익명 라벨 4개, 조사형·표면 명사·한 글자 상품 3개를 추가합니다. PC2 원 실행기 자체의 after 대조는 이 테스트와 별도로 실행합니다.

```powershell
$env:PYTHONUTF8 = '1'
.venv/Scripts/python.exe -m pytest -q tests/test_multi_request_provenance.py tests/test_request_grounding.py tests/test_analysis_repair.py tests/test_analysis_grounding_regression.py tests/test_analysis_semantics.py tests/test_receipt_review_guidance.py tests/test_two_flow_asgi_workflow.py
```

### 첫 보완 재검과 추가 실패

- 메인 7파일 첫 보완 실행은 **275 PASS /1 FAIL /250 subtests PASS**입니다. 새 한 글자 상품 대조군에서 `컵을 반송해 주세요.` 뒤 `컵 반송 요청만은 취소합니다.`가 아직 활성으로 남았습니다.
- 독립 검토자의 PC2 동일 원 실행기는 **20/20**, 기존 고정15개는 **15/15**, R1은 **31/31** 일치했습니다. 원래8개 실패가 해결됐다는 범위의 결과입니다.
- 별도 독립 일반화18개는 **17/18**, G04 `교환 방법 요청만을 취소합니다.`에서 독립 재배송 요청까지 제거됐습니다. 결합 조사를 같은 문법으로 처리하도록 보완했습니다.
- 별도 명사 경계3개는 **1/3**, J01은 위 컵 실패이고 J02는 실제 다른 상품 `파이`/`파`의 취소를 합치는 오류였습니다. 독립 검토자의 고정 `f043c60` 재현에 따라 J02는 새 회귀가 아니라 **첫 수정에서도 남은 기존 오철회**로 구분합니다. 첫 수정에서는 비교형에 `파이`와 `파`가 함께 들어가 오류가 남았으므로 `이/가` 말음을 무조건 주격 조사로 제거하지 않도록 보완했습니다. J03 `파이를`/`파이` 동일 상품 철회는 통과했습니다.

명시된 작업·상품의 동일 여부는 이제 참/거짓/문법 범위 밖으로 구분합니다. 명시 비교에서 일치한 한 글자 상품을 기존 길이2이상 어휘 겹침 검사로 다시 버리지 않습니다. 이름을 비교할 수 없는 문장을 일반 한국어 의미 해석까지 해결했다고 주장하지 않습니다.

이후 같은 상품의 조사 대조군에 파이를 추가하고, 다른 상품 파이/파 반례와 두 독립 요청의 결합 조사 변형8개를 고정했습니다. 제품 수정 뒤 `git diff --check`는 통과했습니다. 메인이 위 동일 7파일을 최종 재실행한 결과는 **286 PASS /250 subtests PASS /5 deprecation warnings,9.09초,exit 0**입니다. 기존233개에 새 고정/경계53개가 추가된 결과이며 처음 실패한 실행과 구분합니다.

독립 검토자의 최종 재검은 **PC2 원 실행기20/20, 기존 고정15/15, R1 31/31, 일반화18/18, 명사 경계3/3** 일치입니다. G04/J01/J02가 같은 입력·기대값으로 해소됐고 공개 schema의 `c20d411` 대비 불변·입력/실행 중 소스 불변·외부 호출0도 확인했습니다. 각 분모는 서로 다른 검증 묶음이며 하나의 새 정확도 점수로 합치지 않습니다. 상세 독립 증거는 [메인 인수 보고](pc2-r2-main-intake.md)에 기록합니다. 구현·테스트는 이 통과 상태로 동결했습니다.

## 한계

한국어 형태소 분석·화자 신원 확인 전체를 구현한 것이 아닙니다. 명시된 제한적 작업·상품 구조의 비교 규칙이며 불명확한 반복 출처와 모호한 지시어 정책은 유지합니다. 새 모델 평가·과금·브라우저·배포·사람 청취를 수행하지 않았고, 이전 실제 canary의 12필드 중 8 PASS /4 FAIL 결과를 바꾸지 않습니다. 09:00 KST 이후에는 신규 구현을 시작하지 않습니다.
