# N04-Q3 독립 복구 검수 계획

2026-09-21 21:25 KST / pc4 장준호 / j324rst-svg. 제품 `c6f734f60d0dbe0151a69e35b8dfd8a41f3c5bd5` 고정. 별도 깨끗한 `happycall-q3-c6f734f` checkout, 승인 `demo-media-20260921-audio-v2` 자산 3건만 사용합니다. 초기 실행 전이며 8축 PASS는 아직 0입니다.

작은 첫 검증 단위: 실제 서버 POST201을 받은 뒤 브라우저 응답만 차단하고 조회404만 의도적으로 주입합니다. 같은 키·같은 본문 재시도의 실제HTTP201/접수 ID/총수/revision/history를 대조해 중복0을 확인합니다. 성공 응답은 합성하지 않습니다.

| 축 | 독립 입력과 기대 |
|---|---|
| Q3-01 | 두 사례 상담·센터 고유 초안, 추가/미추가 조치; WMS/TMS·사례 왕복 뒤 역할/사례별 보존·서버 미저장 |
| Q3-02 | 다른 담당자 실제 PATCH 후 목록 갱신; 자동 덮어쓰기0, 서버/초안 대조, 명시 선택 뒤 저장 |
| Q3-03 | 실제 POST201 응답 유실→조회404 주입→동일키/본문 UI 재시도; 신규 정확히1건 |
| Q3-04 | UI에서 생성한 키·다른 본문 실제HTTP 요청409; 원접수·revision·history·전체 목록 불변 |
| Q3-05 | 실제 PATCH200 뒤 응답유실; 동일내용 GET 확인과 후속 다른담당자 수정 대조를 각각 관측 |
| Q3-06 | 서버에 전달하지 않은503 반례; GET200을 저장 성공으로 오인하지 않음 |
| Q3-07 | 명시keep 직후 제3자 실제PATCH; UI재시도 실제409·초안보존·재대조 |
| Q3-08 | 실제저장 응답보류/유실, 메뉴왕복·편집/중복저장잠금·390px keyboard 선택 |

각 축 새 임시 저장소와 browser context, 실제 same-origin HTTP/정적production export를 사용합니다. 부모 q3-run.py가 제품/빌드/검사기 해시, 원래 상태 보존, 외부/과금0과 자기API종료를 검사합니다. 상담 상태는 공개 HTTP를 통한 기록된 setup/다른담당자 수정만 허용합니다. 원장 직접 편집은 하지 않습니다. 공통 참고41검사·이전Q2음성6회 실적은 합산하지 않습니다.

빌드는 기존 q2-build.py의 동일출처 빌드 도구를 재사용했습니다. 새 source fingerprint에는 외부fixture·manifest·TMS overlay가 포함되고 전후 제품67개 입력을 대조합니다. [계약](q3-c6f734f-contract.json), [빌드 기록](q3-c6f734f-build.json). fixture/manifest는 Gitblob과 바이트가 같고 LF입니다.

과거 Q2 실패 검사기31f1ffd2…의 별도 원본 백업은 확보하지 못했습니다. 남은 것은 당시 해시와 실행 증거이며 재구성본을 원본이라 공유하지 않습니다. 이번 Q3는 실행 전 tester-source/에 실제 모든 검사기 파일을 원본 바이트로 복사하고 해시를 보존합니다.

현재 제한: 자동화/합성/격리API 검증이며 실제AI·사람검증·배포·브라우저 강제종료 영구복구는 포함하지 않습니다. TEST왕복 별도대기. 중앙표/제품/자산 변경0, 인수는 pc1입니다.

실행 후 결과: [고정 후보 최종 보고](q3-c6f734f-results.md). 위 초기 0 PASS는 계획 작성 시점의 기록이며, 세 실행과 P2 관측을 최종 보고에 분리했습니다.
