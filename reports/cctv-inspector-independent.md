# CCTV 조사 컴포넌트 독립 검증

2026-09-21 21:44~21:46 KST, pc1/CJJ의 읽기 전용 적대 검증자. 대상 TSX SHA는 `adacddee3ad0eaf0a77ee269b75a8383de019e442cce32ea9baf14d91ffecfb1`, CSS `59ad04efe37639727902184bdeb973812df4fcb36f883d33db37aea6c38fdb61`, validator `d3e7c9a3e9de71e5b487efa3e5057e3dc5aad36f2f0b104dddb389575c9336e5`입니다. 실행 전후 해당 제품 파일이 같았습니다.

## 판정

P2 한 건을 제품 함수 실행으로 발견했습니다. 전체 자산 끝 `currentTime=12`, 24fps/288프레임에서 ‘이전 한 프레임’이 **288→288**로 남았습니다. `floor(12*24)=288`은 이미 유효 index 최댓값287을 넘기 때문에, 방향 적용 전에 현재 index를 제한해야 합니다. 부분 구간 끝은 **145→144**, 시작점 다음은 **1→2**로 동작했습니다. 브라우저 버튼을 누른 재현과 구별합니다.

실제 TSX를 TypeScript `transpileModule`로 변환하고 `function step(direction)`부터 다음 함수 앞까지 추출해 `new Function`의 제한된 video/tracks/clip/seek 대조 객체로 실행했습니다. 첫 정규식 추출은 검사기 오류로 exit1, 추출 경계를 수정한 뒤 아래 결과를 얻었습니다. 제품 함수 본문은 바꾸지 않았습니다.

```text
full-end-previous: before=288 after=288 time=11.958333333333334
partial-end-previous: before=145 after=144 time=5.958333333333333
first-next: before=1 after=2 time=0.041666666666666664
```

계약 순수 실행은 정상1건 허용, 다른 사건·도크·이벤트·단계 역전·시간 오류·프레임 누락·해상도 불일치7건 차단입니다. 실제 `verifiedBytes` 함수에 통제 응답을 넣어 정상 허용, 동일 크기 해시 오류·크기 오류·404 거부를 확인했습니다. 실제 다운로드/네트워크나 원천자료 전송을 한 검사는 아닙니다.

## 21:51 수정본 독립 재검

수정 TSX SHA `dafe4f74a6ba0816a7ed8061288f9a040ab86c54e737e398dba3b9a6dc6b2395`를 실제 TypeScript AST로 읽어 `step`·`seek`와 실제 validator의 `frameAt`을 독립 실행했습니다. 실행 전후 SHA가 같았습니다. **정상·경계·좌표 없음 9/9, 회귀 변이 2/2 검출**입니다. 전체 끝 이전은 **288→287 / 11.9166667초**, 부분 끝 이전은 **145→144 / 5.9583333초**입니다. 3.01초 비프레임 구간 시작에서 이전을 눌러도 3.01초를 유지했습니다.

현재 index 제한 제거 변이는 288→288을 재현했고, seek 구간 제한 제거 변이는 등록 시작 3.01초보다 이른 3.0초로 벗어나 모두 검출됐습니다. 구현자가 만든 14개 순수 함수 검사를 다시 합산하지 않았습니다. 독립 프로브의 서버·브라우저·네트워크·파일 쓰기는 각각 0건입니다. P2는 **제품 함수 경계 범위에서 해결**됐으며 수정 SHA의 브라우저 재검은 NOT_RUN입니다. 이전 SHA의 64/64 결과는 원본 그대로 유지합니다.

## 브라우저 범위와 실행 거부

독립 검증자가 PowerShell here-string을 `node --input-type=module`로 전달하여 메모리 HTTP 서버 `listen(0,'127.0.0.1')`와 `chromium.launch(headless:true)`를 새로 만들려던 **exec_command가 자동 승인 검토에서 거부**됐습니다. 원문은 `CreateProcess ... rejected: blocked by policy`, 세부 사유는 제공되지 않았습니다. Wall time0.0초였고 명령 실행·포트 할당·서버/브라우저 생성은 없었습니다. 이 경로는 중단했으며 다른 도구·세션으로 우회하지 않았습니다.

이후 순수 코드 실행과 기존 실행 증거 대조만 수행했습니다. `.local/cctv-inspector-1789994429642/results.json`의 60/60 및 실제12초 1× 자연 종료를 대조했으나, 구현자의 실행이며 독립 브라우저 재시험으로 세지 않습니다. 이60건의 전체12초 검사는 noTracks 모드여서 발견한 끝 프레임 경계를 포함하지 않습니다. 뒤의64건 실행과도 분모를 섞지 않습니다.

독립 브라우저에서 지연 fetch·닫기·사건 변경·초점/전체화면·3폭은 미실행입니다. 960×540 기존 영상과 1280×720 새 좌표의 실제 정합도 아직 검증하지 않았습니다. UI 테스트 좌표의 기능 통과를 실제 물류 객체 추적이나 귀책 판정으로 확대하지 않습니다.

메인은 P2를 구현자에게 반환했고 위 21:51 수정본의 독립 순수 경계 검사를 받았습니다. 수정 SHA의 브라우저 재검과 최종 전체 서비스·새 장면의 통합 인수는 미완료입니다.
