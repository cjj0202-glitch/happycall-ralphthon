# PC4 N04-Q3 증거 인수 — c6f734f 고정

2026-09-21 21:53 KST / pc1 CJJ의 독립 읽기 전용 검토. **PC4의 고정 제품 복구 검수 8축·297개 명시적 관측은 제출된 실제 값과 일치하여 범위 한정 인수가 가능합니다. P2 성공 알림 잔류는 유효합니다.** 현재 main, 새 CCTV 검사기, v3 음성의 통합 실행 인수는 아닙니다. N04 전체 완료나 배포 완료로 처리하지 않습니다.

- 제출 커밋: `d7841717e8181d4f9315a20dee5099c073251019`.
- 실제 시험 제품: `c6f734f60d0dbe0151a69e35b8dfd8a41f3c5bd5`.
- PC4 최종 실행: 2026-09-21 21:30:23.564–21:30:54.084 KST.
- 원격 산출물: [결과 원문](https://github.com/cjj0202-glitch/happycall-ralphthon/blob/d7841717e8181d4f9315a20dee5099c073251019/reports/pc4/q3-c6f734f-results.md), `reports/pc4/q3-run-20260921T123023563161Z/`.
- pc1 검증 원본: `.local/pc4-q3-main-intake/verify.py`, `verification.json`. `git show <SHA>:<path>`로 원본 바이트를 읽었으며 checkout·브랜치 변경·제품 수정·커밋·외부 전송은 하지 않았습니다.

## 실제 대조 결과

재현 명령:

```powershell
.venv/Scripts/python.exe .local/pc4-q3-main-intake/verify.py
```

명령 종료코드 0. 검증 판정 12/12이며, 다음 실측 분모를 따릅니다. 이를 사용자 시나리오 12건이나 새 브라우저 실행으로 세지 않습니다.

| 대조 대상 | 실측 |
|---|---|
| PC4 Git 바이트 대조표 | 명시 파일 98/98의 크기·SHA-256이 실제 제출 Git blob과 일치 |
| 3회 실행 전 보존한 검사기·helper | 11개씩 총 33/33이 해당 실행의 시작/종료 SHA와 일치 |
| 고정 제품 입력 | 67개 중 40개 Git blob 직접 일치, 27개는 LF→CRLF 변환 바이트와 일치. 그 밖의 내용 차이 0 |
| 실행 전후 제품·빌드 | 세 시도 모두 동일 제품·source/output 지문이며 각 releaseStart와 releaseEnd 일치 |
| 최종 실행 분모 | 서로 다른 합성 저장소 8개, 고유 축 8개, actual/expected/pass 재대조 297/297 |
| 빌드 기록 | recordDigest 재계산 일치, sourceBefore=sourceAfter 및 계약 source/output 일치 |
| 자산 기록 | v2 승인 자산 3개의 계약 SHA와 원격 실제 HTTP SHA·Range206 관측 일치 |
| 집계 가드 대조 | 기존 `aggregate_guard` 함수만 AST로 추출: 정상 1건 허용, source 변경·축 누락·저장소 중복·관측 누락·identity 변조·과금 호출·pageerror 7건 차단, 8/8 |

source fingerprint는 `4d6d0f6b9dc00b874efbb2549d7ffc5369cfdde62af86f3f36cd8d2b1764a9b8`, output fingerprint는 `3d1be22730f4f84d1e7933099818f3ee6dd354b149774cdad4558fbe1623eab7`입니다. **PC4의 실제 output 26개와 임시 서버 저장소 바이트는 이 검토 위치에 없으므로 output 전체를 pc1에서 다시 해시하거나 실행한 것은 아닙니다.** 보존된 제품·검사기 바이트, 빌드 기록, 실제 HTTP 관측, 화면을 대조한 결과입니다.

## 8축과 HTTP 관측

| 축 | 독립 대조한 관측 수 | 인수 범위 |
|---|---:|---|
| Q3-01 | 57/57 | 상담/센터, 사례별 초안·추가/미추가 조치의 메뉴 왕복 보존 |
| Q3-02 | 37/37 | 다른 담당자 PATCH 후 자동 덮어쓰기 차단, 대조와 명시 선택 |
| Q3-03 | 42/42 | 실제 POST201 저장 후 응답 유실·선언된 조회404, 동일 키/동일 본문 재시도, 신규 정확히 1건 |
| Q3-04 | 17/17 | UI 생성 키를 이용한 다른 본문 공개 HTTP 요청의 실제409와 원접수 불변. UI409 표시는 제외 |
| Q3-05 | 50/50 | 실제 PATCH200 저장 후 응답 유실 두 건, 동일 저장 조회 확인과 후속 수정 대조 |
| Q3-06 | 24/24 | 서버 미전달503 주입 후 GET200을 저장 성공으로 오인하지 않고 초안 보존 |
| Q3-07 | 35/35 | 명시 초안 유지 후 제3자 수정, 실제409 두 건, 대조한 revision만 사용 |
| Q3-08 | 35/35 | 응답 보류·유실 시 편집/추가 저장 잠금, 사례별 분리, 390px 키보드 조회 복구 |

`results.json`의 HTTP 관측 행은 92개입니다. 요청 중단은 성공 응답을 조작하지 않고 `route.fetch()`로 받은 실제 응답 뒤 `route.abort()`하는 코드와 원격 응답 본문·revision을 대조했습니다. 조회404 및 미저장503은 명시적인 실패 주입이며, 실제 서버의 404/503 발생으로 바꿔 해석하지 않았습니다. 센터 선행 상태와 다른 담당자 변경은 공개 HTTP setup이고 UI 조작과 구분되어 있습니다.

pageerror 0, setup 오류 0, analyze 요청 0, 외부 시도 기록 0. 콘솔 오류는 **8건**이며 응답 유실4·조회404 1·미저장503 1·실제409 2의 선언된 경로와 맞습니다. 297개는 `check()`의 명시 관측 수이며 검사기 안의 추가 assert까지 포괄하는 전체 assertion 수로 표현하지 않습니다. 검사기는 `assert.deepEqual(actual, expected)` 실패 시 그 축을 FAIL로 기록합니다.

## P2 알림 잔류 확인

최종 실행의 `Q3-08-q3-08-uncertain-390.png`를 pc1에서 직접 열었습니다. 녹색 **“상담원이 편집한 접수 정보를 저장했습니다.”**와 주황색 **“저장 여부 확인이 필요합니다.”**가 같은 화면에 있습니다. 이 화면은 원래 390×3536이며 검토 도구의 축소 표시에서 양쪽 문구를 확인했습니다.

원격 실제 저장 관측은 revision1 `12:30:50.339094Z`의 응답을 UI에 전달한 뒤 revision2 `12:30:50.779156Z`의 응답만 유실시켰습니다. 0.440062초 간격이며, 첫 성공 알림의 6초 유지시간 안입니다. 고정 제품 `apps/web/app/page.tsx` 45행의 6초 타이머, 186–194행 상담 저장 및 346–352행 센터 저장 코드가 시작/실패 때 기존 toast를 지우지 않는 점도 `git show`로 대조했습니다.

따라서 **P2는 오탐이 아닙니다.** 이번 관측에서 둘째 저장 자체는 실제로 반영됐고 불확실 상태·편집 잠금·중복 차단은 작동했습니다. “미저장503에서도 직전 성공 토스트가 남는다”는 조합은 원격 실행하지 않았으므로 소스에 근거한 가능성으로만 남깁니다. 메인은 새 저장 시작 시 이전 성공 알림을 제거하고, 상담/센터 각 성공→다른 내용503→GET 원본 및 응답 유실 조합의 회귀 범위를 정해야 합니다. 수정 후 결과를 이 고정 실행에 소급하지 않습니다.

## 실패 보존과 한계

3회 시도는 **3/8 → 7/8 → 8/8**로 원본이 모두 남아 있습니다. 첫 변경은 실제 접근성 이름에 맞춘 요청사항/센터 회신 locator와 관련접수 select 한정, 두 번째 변경은 form의 단일 textarea 한정입니다. 보존 소스 diff에서 기대값이나 업무 판정의 완화는 확인되지 않았고, 제품·빌드 변경도 없었습니다. 최초 추출 후 콘솔 출력에서 Windows cp949 인코딩 오류가 났으나 파일 98개 추출은 완료됐습니다. stdout UTF-8 설정 뒤 내용을 읽고, 최종 검증은 실제 Git blob을 다시 읽어 성공했습니다.

현재 main과 다른 음성/영상에 결과를 적용하지 않습니다. 실제 사람 관찰·실제 AI 호출·최종 배포·브라우저 종료 뒤 초안 복구·N04 전체·TEST 왕복 완료를 증명하지 않습니다. Q2의 과거 미보존 검사기 이력도 이번 Q3의 33개 보존으로 소급 해소하지 않습니다.

상위 작업에서 임시 서버·브라우저 실행이 자동 승인 검토로 거부된 경계를 전달받아 이번 검토에서는 새 서버·브라우저 실행을 요청하지 않았습니다. 우회 실행도 없으며, 기존 원격 관측과 순수 파일·코드·해시 검증으로 인수 범위를 제한했습니다.
