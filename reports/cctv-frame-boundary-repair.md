# CCTV 프레임 경계 조작 검증 — 2026-09-22

pc1/CJJ, 수정 전 HEAD `5e3095f`. 다음 프레임 버튼을 눌렀을 때 마지막 프레임의 표시 시간이 뒤로 이동하던 문제를 수정했습니다. 실제 영상에 대한 귀책 판단이나 AI 검출 기능을 추가한 것은 아닙니다.

## 재현·수정·재검

독립 검토자는 실제 288프레임 tracks와 production TS 함수를 실행해 전체 끝 `12.000 → 11.958333`, 부분 끝 `6.010 → 6.000`을 관측했습니다. 프레임 번호는 각각 288·145로 같았습니다. 기존 테스트는 첫 값을 기대했기 때문에 시간 역전을 발견하지 못했습니다.

메인은 설계에 방향별 시간 단조성 계약을 먼저 추가하고 기존 source에서 새 검사를 실행했습니다. `node tests/e2e/cctv-inspector-step-unit.mjs`의 수정 전 결과는 고정 **15/20**, 방향·구간 **75/96**, exit 1입니다. 결과는 `.local/cctv-step-unit-1790005944223/results.json`에 보존했습니다.

`step`은 다음/이전 프레임 목표를 계산한 뒤 현재 시각과 방향을 대조합니다. 다음 버튼은 시간을 줄이지 않고, 이전 버튼은 늘리지 않습니다. 영상 구간 제한과 pause, 좌표 미검증 시 무동작을 유지했습니다.

| 수정 후 검사 | 실측 |
|---|---:|
| 실제 TS step/seek 고정 사례 | 20/20 |
| 24·25·30 fps × 전체/부분/짧은 구간 × 시각·방향 | 96/96 |
| 마지막 인덱스 제한 제거 변이 | 검출 1/1 |
| 방향별 시간 보존 제거 변이 | 검출 1/1 |
| 기존 trusted tracks 검사 | 49/49 |
| `npm run typecheck` | exit 0 |

수정 후 결과: `.local/cctv-step-unit-1790005968016/results.json`, `.local/cctv-trusted-tracks-1790005970466/results.json`. 두 검사와 타입검사는 서버·브라우저를 시작하지 않았고 유료 모델을 호출하지 않았습니다. source `CctvInspector.tsx` SHA256은 `cf0531117df400376a33bc6f508113381d7811e971220437e7ac513d9ae66452`입니다.

별도 검토자는 같은 SHA의 실제 함수를 TypeScript AST로 추출해 `node --input-type=module -`에서 실행했습니다. 기대 프레임은 기존 테스트 계산식을 복사하지 않고 프레임 시각 목록을 열거하여 대조했습니다. 24fps 전체 끝의 `+,+,-,+,+`, 25fps/37프레임과 30fps/361프레임, 프레임 경계를 걸치는 2ms 및 한 프레임 안의 8ms 구간 등 **6시나리오·32/32 조작**에서 시각 방향·구간·setTime·pause를 확인했습니다. 실제 24fps 전체 끝 시퀀스의 시간은 `12 → 12 → 11.916667 → 11.958333 → 11.958333`, 프레임은 `288 → 288 → 287 → 288 → 288`입니다. 새 결함은 발견하지 못했고, 픽셀·디코더·React effect는 실행하지 않았습니다.

## 렌더 원본과 미검증 범위

실제 full1080 후보 tracks는 158,546 B, SHA256 `b9b9f50295af799cfdf718b840b0cef703f57ad3e3b778557fcd9fdeb9ac681c`입니다. 수정 전 독립 검토에서 시각→프레임 288/288, 경계 8/8, 실제 JSON의 사건·시각·개수·객체·업무 토트·좌표 변이 거절 6/6을 확인했습니다. JSX 객체 선택·종료점·해제 3/3과 tracks 바이트 해시 정상/오해시 2/2는 메모리 상태·fetch double 실행입니다. 실제 MP4 identity는 대용값이므로 영상 해시 연결 성공으로 세지 않습니다.

메인은 렌더 원본 `frame-0160.png`(1920×1080)를 열어 슈트 위 상자가 보이는 것을 확인했습니다. 해당 tracks의 경과 시각은 6.625초, 경계 좌표는 x938.12–1064.12/y636.52–762.89px입니다. 단일 정지 화면의 위치 대조이며 픽셀 경계 오차 측정이나 전체 288프레임 가림 검증은 아닙니다.

현재 제품에는 v3 영상이 등록되어 좌표/프레임 이동이 비활성 상태입니다. 새 1080p MP4 등록·최종 영상 디코딩·브라우저 프레임 조작·움직이는 영상과 bbox 동기화는 렌더/패키징 완료 후 별도 검사합니다. 이번 수정은 기존 00:41 생산 빌드 이후이므로 그 빌드 통과로 이번 변경을 증명하지 않습니다.
