# N03-M6 보완 — 영상 좌표계와 성공 보고서 발행 경계

pc3 LAPTOP-U2AL73UH/mcjun86-oss · 기존 #9 · 제출 `3a223f160941212d4d8608a53806563ec2ef9318`. 메인 기준 `4b18b27e00ffab3106d1d88920e4f18b3de64356`. M6는 아직 main 통합 전이며 이 카드는 같은 작업의 검토 보완입니다.

## 결과와 현재 증거

메인은 격리 사본의 최종 20개 unittest를 실행해 **20/20 PASS, 267.557초, exit 0**을 확인했습니다. 원격 19개 전체 실행 이후 추가된 SAR 검사를 포함한 최종 소스입니다. 5개 신규 파일의 Git blob/실행 사본 바이트도 5/5 일치합니다. 최초 실행은 메인 격리 폴더의 기존 fixture 2파일 누락으로 setup이 실패했고, 원본 의존 파일을 복사한 뒤 재실행했습니다. 이 환경 준비 실패를 제품 결함으로 세지 않습니다.

별도 읽기 전용 검토자가 아래 P2 후보를 메모리 실행으로 재현했습니다. 실제 1080p 장면이 잘못됐다는 판정이나 실제 Windows 파일 잠금 재현은 아닙니다. 원래 정상 검사 통과와 추가 반례를 모두 보존합니다.

## 보완할 사용자 결과

1. 원본 tracks를 연결할 영상의 표시 좌표계가 변환되지 않았음을 확인해야 합니다. `_input_metadata`에 `displaymatrix: rotation of -180.00 degrees`, `_probe_json`에 `rotation=-180`, 인공 MP4에 180도 `tkhd` 행렬을 넣어도 각각 통과했습니다. 실제 생성 명령이 이런 영상을 만들었다는 뜻은 아닙니다. 정상 항등행렬/무회전은 통과하고 비항등 회전·변환 또는 검증 불가 정보는 거절하도록 보완하세요. 기존 진짜 FFmpeg 출력으로 양성 경로가 유지되는지 대조하고 관련 정상/음성 fixture도 함께 고칩니다.
2. 현재 성공 report를 쓴 뒤 마지막 입력/출력 검사를 합니다. 그 검사 실패와 마커 `unlink()`의 `PermissionError`를 함께 주입하면 함수는 FAIL인데 디스크 관점에는 `candidate-report.json=PASS_WITH_PENDING`, descriptor, `failure.json=FAIL`이 함께 남습니다. 삭제 허용 대조군은 성공 마커를 지웁니다. 최종 성공 보고서 공개 순서와 실패 판정 계약을 보완하고 소비자가 보고서 하나만 보고 성공으로 인수하지 않게 하세요. 보안 설정 변경·파일 잠금 강제 해제는 하지 않습니다. 임의 외부 변조를 영구 방지한다는 보장을 만들지 말고 마지막 검증 이후의 경쟁 한계를 명시합니다.

## 소유·기준

기존 M6의 신규 스크립트 2개와 `reports/pc3/full-media-*`만 편집합니다. 기존 생성기/M5 검사기/실제 렌더 입력/제품/UI/manifest/Release/공통 원장은 수정하지 않습니다. 모든 기존 변경과 제출 커밋을 보존하고 같은 branch에 후속 커밋으로 보냅니다. pc1의 렌더 PID13052는 01:13:15에197/288이며 중단·재시작하지 않습니다.

## 검증·기대값

먼저 위 두 주장을 원본에서 독립 재현하거나 반증하고 출력 근거를 남깁니다. 정상 무변환 영상 및 정상 성공 보고서 양성, 회전/행렬 변형 음성, 마지막 검사 실패와 cleanup 실패 복합 반례, 관련 가드 제거 변이를 포함합니다. 완성 291파일은 인공 테스트 입력만 사용합니다. 전수 suite 최종 소스로 재검하고 실제 설치 도구 사용은 원래 카드의 허용된 작은 인공 PNG 범위에서만 합니다. 실제 메인 장면이나 최종 제품 전체 검수로 합산하지 않습니다.

## 인계·중단

수정 가능 범위와 실제 관측에 따라 상세설계부터 갱신하고 명시 소유 파일만 커밋·push/원격SHA 대조 후 #9에 재현 명령·기대/실측·결과SHA·한계를 회신하세요. 첫 결과 목표01:40 KST입니다. 새 renderer/server/browser·설치·권한 변경·정책 차단 우회·실제 소스 데이터 공유는 금지합니다. 해결되지 않은 반례는 남겨 두고 메인이 범위 인수 여부를 결정합니다. 전체 N03/실제 영상/배포 완료나 이슈 종결은 하지 않습니다.
