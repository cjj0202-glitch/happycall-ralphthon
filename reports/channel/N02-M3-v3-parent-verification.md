# N02-M3 — v3 통화와 실제 상담 작업대 연결 검수

## 왜 지금 / 기준

pc2의 `6a13abe`를 메인이 수신해 증거 Git blob13·측정제품6·발화PCM8 = 27/27 대조했습니다. M2의 격리 재생18/18 결과와 음원 보존은 해당 범위로 인수합니다. 메인은 v3 Release를 실제 발행·독립 다운로드3/3 검사하고 정본 통화/시각을 적용했습니다. N02 전체나 사람 청취 완료를 뜻하지 않습니다.

이번 고정 제품 기준은 **92d2ecbad981f366a9e5ffc850d9c2bb7fd5d3a2**입니다. `planning/media/audio-pause-adoption.md`, `reports/media/pc2-pause-adoption.md`를 읽습니다. PC4는 별도 c6 고정 복구 검사 중이므로 그 제품/자산을 바꾸라고 지시하지 않습니다.

## 사용자 결과 / 할 일

경영주 통화가 끝난 뒤 상담원이 실제 부모 작업대에서 분석·접수 대조를 이어갈 수 있어야 합니다. 관측용 가짜 분석 버튼 대신 실제 page/서버를 사용하되 **replay 모드**로 검사합니다.

- 두 승인 통화 각각 1회 1배속 자연 종료 → 실제 분석 버튼 활성화 → replay 분석 완료 → 접수 양식/원문/추가 질문 대조.
- CASE-0002 음원 버전 URL·49.75초·발화4~8 +0.20초, 같은 id에서 음원 URL이 바뀌면 완료 상태 초기화.
- 끝 seek/구간 재생으로 분석 준비 완료가 되지 않는 반례, 404 또는 디코딩 오류 안내/복구, 분석 응답 실패 후 편집 원문 보존을 각각 실행.
- 390/768/1440의 재생→분석→접수 동선·키보드, 실제 페이지/콘솔 오류를 확인.

## 소유 / 실행

pc2 기존 저장소 `C:/Users/Administrator/Desktop/hackerton/happycall-ralphthon`의 변경을 보존하고, 위 커밋의 별도 clean checkout에서 새 빌드를 만듭니다. 기존 8100/3100이나 다른 세션 서버를 중지하지 않습니다. 빈 새 포트에서 자신이 만든 서버만 종료합니다.

소유는 `tests/remote/pc2/m3/`, `reports/pc2/audio-m3/`뿐입니다. 부모 page/공용 fixture/manifest/API·음원·작업표는 읽기 전용입니다. 결함은 재현 근거와 함께 회신하고 공통 제품을 임의 수정하지 않습니다. 같은 PC 보조 세션을 쓰면 구현·독립 검수 파일을 나눕니다.

새 checkout의 현재 releaseTag는 `demo-media-20260921-audio-v3`입니다. 신규 media 폴더는 `python scripts/fetch_demo_media.py`, 정확한 기존 v2를 보유한 경우만 `--upgrade-approved`를 사용해 원본 백업을 남깁니다. `--verify-only` 3/3 후 진행합니다. 현재 자신이 가진 Python/Node/브라우저 경로를 확인해 사용하고 pc1 절대 도구 경로를 복사하지 않습니다.

## 검증 방법 / 기대값

1. 실행 전 제품 SHA·빌드 source/output fingerprint·음원3 SHA·실제 checker/helper 원본 바이트를 결과폴더에 보존합니다.
2. 자연 종료는 실제 decoded duration, rate1, played 시작~끝, trusted ended1, 실제 경과시간을 기록합니다. 47.15초/49.75초보다 빠른 seek를 완주로 세지 않습니다.
3. 실제 API replay 호출/응답과 화면 활성화·접수 상태를 연결합니다. STT/GPT 라이브 호출0·유료호출0입니다.
4. 정상/누락·실패/복구/3폭을 별도 분모로 기록하고 제품 소스 전후 해시를 대조합니다. M2 격리 결과나 PC4 Q3 결과와 합산하지 않습니다.

## 인계 / 중단 조건

첫 검수 계획·반례 10분 내, 첫 결과 20~30분 내 같은 #8에 회신합니다. 기대/실측·실패→수정(검사기인지 제품인지 구별)→동일 재검·커밋·미실행을 전달합니다. 자기 인수·TODO 체크·이슈 종결은 하지 않습니다.

권한 거부·신원 불일치·자산 해시 불일치는 중단하고 정확한 사유를 회신합니다. 기존 설정/보안/서비스를 바꾸거나 다른 도구로 거부를 우회하지 않습니다. 사람 청취·실운영 연동·최종 배포·전체 6회 인수는 이 카드 범위가 아닙니다.
