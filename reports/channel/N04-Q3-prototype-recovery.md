# N04-Q3 / pc4: 새 시제품의 초안·저장 복구 독립 검수

## 결론·왜 지금

pc4 장준호 / j324rst-svg의 `ca00b2fb23b5f2e252dba5c0930f6aea3a8416c5` 결과를 pc1이 대조했습니다. **bfc8543 후보의 6/6·15/15 검수는 그 버전에 한정해 인수합니다.** 코드·입력·음성·영상·PATCH 증거220조건을 대조했으며 새 UI에 소급하지 않습니다. 첫 실패 당시 checker SHA `31f1ffd2…`의 원본은 두 공유 커밋에서 확인되지 않아 P2 보완으로 남습니다. 로컬 원본이 있으면 그대로 보존·공유하고, 없으면 재구성본을 원본이라 하지 마십시오.

이번 제품 기준은 **`c6f734f60d0dbe0151a69e35b8dfd8a41f3c5bd5`**입니다. 서버 멱등 접수·화면 이동 초안 보존·저장 미확정 복구가 새로 들어갔습니다. 현재 main에서 진행 중인 AI 요청 정제 수정은 이 카드에 혼합하지 마십시오. 실제 저장소는 기존 `C:/Users/j324r/OneDrive/문서/ChatGPT/해커톤_D-Day/happycall-ralphthon`이며 본인 실제 보고와 다르면 먼저 확인합니다.

## 해줘야 할 일·소유

기존 브랜치에서 독립 tester·`reports/pc4/q3-*`만 작성합니다. 제품은 위 SHA의 별도 깨끗한 사본에 고정합니다. 공통 API/부모 UI/fixture/작업표는 변경하지 않습니다. `planning/prototype-resilience.md`, `reports/prototype-independent-acceptance.md`, `planning/intake-retry-safety.md`를 읽고 **초안 손실·중복 접수·잘못된 저장 성공 표시**를 반박하는 첫 작은 시험을 설계하십시오.

핵심 축은 아래 8개입니다. 초기 상태·요청·기대·실측을 따로 기록하고, 원하는 결과를 얻기 위해 서버 상태를 몰래 바꾸거나 가짜 응답을 성공 근거로 쓰지 않습니다.

1. 상담·센터 작성 도중 WMS/TMS 왕복·다른 사례 전환 후 초안별 보존.
2. 다른 담당자 수정 뒤 목록 갱신: 자동 덮어쓰기 없이 서버/초안 대조와 명시 선택.
3. POST 실제201 저장 후 응답만 유실, 조회404 경계를 거쳐도 **같은 키·같은 본문** 재시도로 신규접수 정확히1건.
4. 같은 키·다른 본문409, 서버 기존접수 불변.
5. PATCH 실제 저장 뒤 응답유실: GET으로 동일내용 확인 또는 다른담당자 후속내용을 대조, 거짓 성공/덮어쓰기 없음.
6. 저장이 발생하지 않은503: 정상GET이 된다는 이유만으로 저장성공 표시하지 않음.
7. 대조 직후 또 다른담당자 수정: 실제409, 초안보존, 재대조 후에만 진행.
8. 저장중 메뉴왕복·응답미확정 중 편집/중복저장 잠금, 390px 키보드 대조 선택.

## 실행 입력·명령

whoami·Git 상태·fetch 후 위 제품SHA와 승인 `demo-media-20260921-audio-v2` 자산3개의 해시를 고정합니다. 본인 Python/Node/Chromium 실제 경로를 쓰고 메인 PC 경로를 복사하지 않습니다. 빌드 소스 지문에 외부 fixture·manifest·TMS overlay도 포함하므로 새빌드를 기록하십시오.

```powershell
python scripts/build_deployment_bundle.py --build
$env:PROTOTYPE_PORT='8941'
$env:PROTOTYPE_OUTPUT='reports/pc4/q3-<본인 실행시각>'
# REHEARSAL_PYTHON과 E2E_CHROMIUM은 pc4의 실제 실행파일 절대경로로 지정
node tests/e2e/prototype-independent.mjs
```

위 공유 검사기는 참고/기준 실행용이며 pc4의 독립8축과 동일 실적으로 중복 합산하지 않습니다. 예시의 출력 이름을 실제 시각으로 바꾸고 8941–8944가 비어 있는지 확인하십시오. helper는 격리 상태·키 미접근·과금0이며 새 Temp를 보존합니다. 다른 기존 서버·감시기를 종료하지 않습니다.

## 완료 기준·시점

첫 반례/설계는 수신 후10분, 전체8축 결과는20~35분 목표입니다. 실제UI·HTTP·case수/revision·초안값·콘솔을 대조합니다. source/checker/출력/자산의 실행 전후 SHA와 LF/CRLF 처리 근거를 남깁니다. 전체 음성6회를 새로 반복하는 카드가 아니라 신규 복구 동작 카드입니다. 외부API·유료호출0, 원본/실패증거 보존, 가짜 성공0을 확인합니다.

## 중단·인계

소유 충돌/권한 거부/원천·키 공개/30분 무진전은 해당 경로 중단 후 같은 #10에 회신합니다. 새검사/증거를 명시 경로로 커밋·push·원격SHA 대조하고, 초기실패/수정/동일조건재검·미실행·제품변경0 여부를 보고합니다. 기준 제품을 바꾸면 그 검사 결과를 별도로 구분합니다. 이슈 전체 종료·N04 최종인수는 새 미디어/최종 배포 검증 뒤 pc1이 합니다.
