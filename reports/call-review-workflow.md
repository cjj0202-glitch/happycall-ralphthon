# 상담 작업동선 검수와 수정

2026-09-21 pc1. 독립 화면 검수자가 실제 production export를 격리8831에서 열었습니다. AI Playwright 관찰이며 실제 사람 사용성·청취 평가로 계산하지 않습니다. 메인은 전달받은 명령·실측을 이 문서로 보존합니다.

## 첫 관찰과 수정

52af4c1 통합빌드(source7e76d4b0/outputf6c1841b)에서 재생→분석버튼 거리1440/1024/390폭 각각1787/1985/3902px, 모바일 분석전문서6701px·분석후7941px였습니다. 같은AI요약2회가 펼쳐져 보였습니다. 기능6회통과와별개로 핵심작업동선P1로 판정했습니다.

부모가 소유하는 analysisActions/intakeEditor를 CallReview의 렌더 슬롯으로 전달했습니다. 음성 바로뒤 분석, 데스크톱 오른쪽 편집, 모바일 분석다음편집으로 배치하고 원문·화자대화록·상세AI대조는 접어서 보존했습니다. 상태/재생/실제API/확인게이트는 바꾸지 않았습니다. 분석실패와 재시도는 상세접기 밖에 유지했습니다.

v2 검수빌드 source1170903e/output3d5c1dcb 실측:

| 폭 | 재생→분석 | 편집 시작 | 본문 가로 넘침 |
|---|---:|---|---:|
|1440|100px|오른쪽 x730/y639|0|
|1024|155px|오른쪽 x522/y661|0|
|390|168px|분석버튼 아래113px|0|

390px 분석전문서3158px로 줄었고 3폭 실제PNG를 직접확인했습니다. 원문373자/API일치·두사례8발화보존, Enter펼침/Space닫힘/포커스확인, 현재편집값 상세대조동기화, 확인체크true문구1→재편집false/문구0을 실측했습니다.

CASE1 실제47.15초를1배속/볼륨100%/음소거해제/연속played0–47.15로 재생(벽시계47.322초)했습니다. 분석POST503을1회주입했을때 메모보존, 오류Y1454/재시도Y1482로 보였습니다. 재시도실제격리API revision0→1/replay성공·오류해제, CASE2전환후음원교체/49.55초/분석비활성/원문접힘/이전음원정지를 확인했습니다. 외부요청0·pageerror0, 콘솔오류1개는 주입503입니다.

재현 측정식:

```javascript
await page.setViewportSize({width:390,height:900});
await page.evaluate(() => [...document.querySelectorAll('button')]
 .filter(e => /처음부터 전체|저장된 분석/.test(e.textContent))
 .map(e => ({text:e.textContent,y:e.getBoundingClientRect().top+scrollY})));
```

검수자 소유PNG4장은 실제열람후 해당검수규칙에 따라삭제했고 브라우저close/isConnectedfalse확인. 따라서 재사용PNG증거가 보존됐다고 표시하지 않습니다. v2에대해P1해소이며 이후타임라인추가v3는별도검사합니다.

## 남은 항목

첫검수의 WMS backdrop닫힘, TMS계획표현/헤더접근성, 일부강조선 P2후보는 이수정의범위밖입니다. 헤더정렬/ARIA속성은 현재정렬선택창·네이티브표와의중복여부를판단한뒤필요한부분만수정합니다. 실제STT/음질/전체대비/배포인수를이화면검사로대신하지않습니다.

v2에서 정본발화에시간정보가없어 구간재생8/8비활성이 관측됐습니다. 이는초기자료누락이며 새접기의회귀가아닙니다. 별도타임라인연결수정·검증은 reports/media/transcript-timing.md에 기록합니다.

## v3 통합 재검증

타임라인이추가된source15d26591/output48ec82d2에서 같은동선100/155/168px·모바일편집113px·3폭넘침0을독립재확인했습니다. 실제16발화구간검사는위별도보고서에있습니다. [리허설결과](e2e/rehearsal-2026-09-21T11-18-13-436Z/results.json):20:18:13~20:21:47KST,전체음성1배속을포함한미도착3+오출고3=6/6,텍스트2/2,오프라인안전1/1,오프라인전체0/1 BLOCKED. 실제HTTP20046/2012/의도4092/의도42224,페이지오류0/외부0/live0,실행전후제품소스변경0입니다. 부모상세대조를실제로펼친후확인해제를검사했고검사를생략하지않았습니다.
