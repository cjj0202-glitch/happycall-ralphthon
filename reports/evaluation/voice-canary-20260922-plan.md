# 현행 시연 WAV 두 건의 실제 전사·정제 사전 계획

2026-09-22 pc1/CJJ. 제목 지시는 `b1ca8f2`에서 이미 보완됐으나 효과를 실모델로 확인하지 않았습니다. 기존 텍스트4건을 또 호출하지 않고, 남겨둔 음성·시연 예산에서 현행 WAV 두 건의 전사→정제를 한 번 검증합니다. 원격 저장503을 우회하는 서버를 만들지 않으며 저장·이관·회신은 실행하지 않습니다.

## 고정 입력과 기대값

입력은 현재 합성 미디어 manifest와 일치하는 아래 두 파일입니다. 재생 배속은 브라우저 선호일 뿐이며 STT에는 원본 WAV를 보냅니다. 국내 TTS 공식 새 음원을 수신한 것으로 표시하지 않습니다.

| 사례 | 실제 파일 | 길이 | SHA256 |
|---|---|---:|---|
| 미도착 CASE-0001 | `apps/web/public/demo/CASE-0001.wav` | 47.15초 | `d4eeb6448ff8969f27034ecade9a050ee212468585c6300b19c9a0ebdc372931` |
| 오출고 CASE-0002 | `apps/web/public/demo/CASE-0002.wav` | 49.75초 | `6fe83afb8552765ab92722b838d2a0905f507551c4f98c58fa62a515033abaa0` |

별도 검토자가 실행 결과를 보기 전에 [사전 oracle](voice-canary-20260922-oracle.json)을 고정합니다. 생성 대본·업무 계약에서 도출하며 fixture의 replayAnalysis를 정답으로 복사하지 않습니다. 대본과 실제 STT, 모델 원응답, 서버 정규화 결과를 구분해 판정합니다.

특히 CASE2는 서로 다른 발화에 단위 정정·잘못 온 상품 처리·주문 상품 처리 요청이 나뉩니다. 단일 requestQuote 계약 때문에 누락될 수 있다는 점을 실행 전에 발견했습니다. 결과를 본 뒤 하나의 요청만 정답으로 완화하지 않습니다. 실제 점포명이 전사에서 달라졌다면 점포 null의 안전성과 전사 정확도 미달을 따로 기록합니다.

## 호출·비용·기록 경계

- 기존 `.local/demo-usage.json`:102건 모두 completed, 예약2890센트, SHA256 `e1551992bd3ae891decbde4ae35e072f2145062a3a7de90026305bdad77a4b82`를 실제로 읽었습니다. 파일/원장/상한을 새로 만들거나 초기화하지 않습니다.
- 두 건 각각 기존 LiveAnalyzer의15센트 예약, 총30센트 후 예상2920/3000센트입니다.80센트를 남깁니다. 이는 보수 예약액이며 실제 청구액이 아닙니다.
- 각 WAV마다 기존 `gpt-4o-transcribe-diarize`1회→`gpt-4.1-mini`1회, 최대 API4호출입니다. 기존 demo_client의 공식 API origin·90초 timeout·max_retries=0을 유지합니다. API 오류면 동일 호출을 반복하지 않고 다음 사례도 중단합니다.
- 실모델에는 실제 STT와 허용된 합성 점포/근거/부서만 제공합니다. 대본·fixture transcript·expected·replayAnalysis·oracle은 넣지 않습니다. intake는 기존 normalizer 비교용으로 서버 메모리에만 있습니다.
- 새 서버·브라우저·로컬 API 재시작, 운영 데이터, 서비스 접수 저장, 원격 환경 설정은 사용하지 않습니다. 기존 화면의22접수와 미저장 초안은 그대로 둡니다.
- 실행 전 HEAD·oracle·WAV·관련 소스9파일의 해시를 고정하고 전후를 대조합니다. 실제 실행 wrapper는 ignored `.local/voice-canary-20260922.py`에 보존하며 실행 전에 무과금 preflight와 차단 대조군을 확인합니다.
- 원응답·STT·실행 입력/원장은 ignored `.local/evaluation/voice-canary-20260922-0415`에만 기록합니다. 비밀값·SDK 예외 본문을 출력하지 않고 공개 보고에는 정제된 기대/실측·해시·오류코드만 적습니다. 같은 경로가 이미 있으면 runner가 다시 실행하지 않습니다.

## 판정과 다음 행동

실제 호출 성공은 품질 통과가 아닙니다. 자동 필드, 제목/요청 의미, STT 의미 보존, 실제 처리·귀책·근거 창작 금지, 필드와 인용의 일관성을 분리합니다. 독립 검토자에게 사전 oracle과 실제 결과의 해시를 함께 전달하고, 실패는 그대로 보존합니다.

이 검사는32개 텍스트 평가·20개 음성 품질·사람 청취·브라우저 전체 업무 흐름·영속 저장·최종 배포를 대신하지 않습니다. 복수 요청이 누락되면 이후 개선은 검증된 개별 인용을 복수로 보존하는 계약으로 검토하며, 제목·단위를 정답처럼 자동 덮어쓰지 않습니다. 추가 과금 재시도는 이 계획에 없습니다.
