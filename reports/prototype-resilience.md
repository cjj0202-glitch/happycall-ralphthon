# 시제품 초안·충돌·저장 복구 검증

**2026-09-21 20:52 KST / pc1 CJJ / 후속 통합 빌드: 54/54 통과.** 상담·센터·경영주 입력이 메뉴 왕복으로 사라지는 문제와, 저장은 되었는데 응답이 유실된 경우의 안내·복구를 수정했습니다. 배포 완료나 실제 사람 사용성 검증 결과는 아닙니다.

- 설계: [prototype-resilience.md](../planning/prototype-resilience.md)
- 재현 검사: [prototype-resilience.mjs](../tests/e2e/prototype-resilience.mjs)
- 실측: [54개 기대·결과와 서버 메타데이터](e2e/prototype-resilience-20260921-2052/results.json)
- 대조 화면: [1440px](e2e/prototype-resilience-20260921-2052/recovery-1440.png) · [1024px](e2e/prototype-resilience-20260921-2052/recovery-1024.png) · [390px](e2e/prototype-resilience-20260921-2052/recovery-390.png)

## 기준과 발견 결함

기준 제품 bfc8543의 기존 빌드를 별도 8841–8844 서버로 복사한 뒤 확인했습니다. 원래 8100/3100 프로세스는 변경하지 않았습니다.

| 실제 재현 | 수정 전 | 수정 후 |
|---|---|---|
| 상담 초안을 수정하고 WMS 왕복 | 저장된 이전 값으로 바뀜 | 같은 사례·다른 사례 초안 분리 보존 |
| 센터 회신과 작성 중 조치를 입력하고 TMS 왕복 | 두 입력 모두 빈 값 | 회신·조치 목록·작성 중 조치 보존 |
| 정상 상태에서 다른 담당자 변경 조회 | 명시적 목록 갱신 버튼 0개 | 목록 새로고침·최종 확인 시각 제공 |
| 실제 PATCH 200 후 응답만 abort | 서버 revision 2로 저장되었으나 재시도 안내, 저장 확인 동작 0개 | 저장 미확정으로 안내·입력 잠금·서버 조회로 복구 |
| 저장 응답을 지연시킨 동안 메뉴 왕복 | 컴포넌트 busy 상태가 해제될 가능성 | 접수별 진행 상태 유지·편집과 중복 저장 차단 |

수정 전 증거는 `.local/prototype-resilience-baseline.json`에 보존했습니다. 초기 하네스에서 중복 label 및 textarea accessible name 선택자 오류가 있었으며, 대상 컨트롤로 선택자를 좁혔습니다. 실패 실행을 삭제하지 않았습니다. 중간 빌드에서는 37/37, 보강 빌드에서는 52/52를 확인했고, 미확정 응답 이후에도 새 편집을 잠그는 2개 검사를 추가하여 최종 54/54를 실행했습니다.

## 구현한 동작

1. 브라우저 페이지 메모리에서 역할·접수별 초안을 분리합니다. 상담 확인 체크·질문 메모, 센터 회신·조치, 신규 텍스트 입력을 보존합니다. 녹음 재생이나 완료 게이트는 캐시에 넣지 않습니다.
2. 목록을 갱신해도 초안과 편집 시작 revision을 교체하지 않습니다. 서버가 바뀌면 대조 화면에서 서버 내용 사용 또는 초안 유지·재확인을 선택합니다. 선택만으로 서버에 쓰지 않습니다. 상담 초안을 유지하면 사람 확인 체크를 해제합니다.
3. POST/PATCH의 응답 유실·시간 초과·5xx·해석 불가 응답을 실패로 단정하지 않습니다. 입력을 보존·잠금하고 현재 서버 값을 먼저 확인합니다. 요청했던 내용과 일치하면 저장된 내용을 확인했다는 안내로 복구합니다.
4. 신규 접수 UUID v4와 본문을 미확정 시도 동안 유지합니다. 조회 404 뒤에도 같은 키와 본문으로만 재시도합니다. 실제 저장을 확인하거나 확정적 거절을 받은 뒤에 입력을 다시 편집합니다. 키 값은 공개 결과 JSON에 기록하지 않습니다.
5. 저장 중 및 응답 미확정 중의 재진입·새 편집을 막습니다. 작성 중인 미추가 조치가 있으면 회신 등록을 막아 조치를 빠뜨리지 않게 합니다. 이관·종결 상태의 기존 역할 잠금을 유지합니다.
6. WMS/TMS도 응답이 유실되면 연결 실패로 단정하지 않고 최신 목록을 조회하여 연결 여부를 확인하도록 안내합니다.

## 최종 실측

실행 시각은 **20:52:10.798–20:52:24.458 KST**, 서버 PID 26276, 포트 8871–8874입니다. 서로 다른 3개 browser context에서 서버 저장소를 공유하고 UI를 조작했습니다.

| 검증 축 | 기대·실측 |
|---|---|
| 3개 독립 context | 경영주 접수 → 상담 실제 PATCH 저장·이관 → 센터 중간/최종 회신 → 경영주 갱신 후 등록 회신·종결 상태 확인 |
| POST 응답 유실 + 조회 404 | 서버 저장 후 응답 차단, 입력·새 전송 잠금, 같은 UUID/본문 재시도, 신규 접수 증가 정확히 1건 |
| POST 응답 유실 + 조회 200 | 저장된 접수를 조회해 복구, 신규 접수 증가 정확히 1건, 확인 뒤 입력 잠금 해제 |
| 상담·센터 PATCH 응답 유실 | 실제 200 저장 후 응답만 차단, 중복 저장·새 편집 차단, GET으로 요청 값 확인 후 복구 |
| 독립 수정과 실제 409 | 기존 초안 유지, 서버의 다른 요청을 덮지 않음, 대조·명시 선택 이후에만 실제 저장 |
| 저장 대기 중 재진입 | 상담 입력·재전송 잠금. 센터 응답을 3초 더 hold한 동안 회신·새 조치·조치 완료·AI 초안 가져오기·재등록 5종 잠금, 응답 후 해제 |
| 1440/1024/390px | 대조 화면에서 수평 넘침 각각 0px, 3개 캡처를 실제 열어 확인 |
| 키보드·새로고침 | Enter로 서버 사용/초안 유지 선택, 초안 유지 시 확인 해제, beforeunload 경고 취소 후 값 보존 |
| 브라우저 pageerror / 외부 요청 | 각각 0건. 검사 대상 54개가 실제 실행됐음을 JSON에 기록 |

브라우저에서 관측한 API 응답은 29개(200 26개 / 201 1개 / 의도한 404 1개 / 실제 충돌 409 1개)입니다. 이 수는 **브라우저가 받은 응답**의 수이며, abort한 응답·`route.fetch`로 확인한 서버 저장·별도 API 사전 조건 요청까지 모두 센 전체 서버 요청 수가 아닙니다. 의도적 오류를 제외하여 0건이라고 표현하지 않습니다.

센터 지연 응답 검사에서 AI 초안 가져오기를 검사하려고 별도 CASE-0002에 비용 없는 replay API와 정상 이관 PATCH로 사전 조건을 만들었습니다. 이 검사는 통화 전체 청취나 실제 STT 검증을 대신하지 않습니다.

## 빌드와 재현

최종 빌드는 외부 fixture·media manifest·TMS overlay까지 포함한 메인의 source fingerprint 보강 이후 실제로 다시 생성했습니다.

| 항목 | 값 |
|---|---|
| build 완료 | 2026-09-21 20:51:42.931 KST |
| source before = after | `a519e346e7a097f00961441373e858a088d86bce119640531fecd10762441292` |
| output fingerprint | `65c40b09aa68ef178eca9d73847c68a92cf237910b65c9040b79c3ba1d2fe40a` |
| output 파일 | 26개 |
| 검사기 SHA-256 | `6adb98e1e3fa17f35a908235f94ff28635b371594390275241936286403b9646` |
| 격리 서버 | paidCalls 0 / keyReads 0 / 기존 budget ledger 접근·생성 없음 |

```powershell
$env:PATH='C:/Users/choi8/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/bin;'+$env:PATH
.venv/Scripts/python.exe scripts/build_deployment_bundle.py --build
.venv/Scripts/python.exe tests/helpers/rehearsal_server.py --port 8871 --metadata .local/prototype-resilience-final.json --preserve-state
# 별도 작업 터미널에서 실행합니다. 앞 명령의 서버 기동 완료를 확인합니다.
$env:RESILIENCE_URL='http://127.0.0.1:8871'
$env:RESILIENCE_OUTPUT='reports/e2e/prototype-resilience-20260921-2052'
$env:RESILIENCE_METADATA='.local/prototype-resilience-final.json'
node tests/e2e/prototype-resilience.mjs
```

재실행은 새 포트·새 출력 경로를 사용하고 기존 결과를 덮지 않습니다. 자기 소유 서버 PID 15516/27092/24592/26276은 각 메타데이터와 CommandLine을 대조하여 종료했습니다. 종료 후 해당 16개 포트의 listener는 0개입니다. 새 Temp 상태와 모든 실패 로그를 보존했고, 기존 8100/3100 프로세스나 과거 보호된 Temp 경로는 건드리지 않았습니다.

## 남은 한계와 인계

공유 전 정리에서 검사 스크립트 끝의 빈 줄만 제거했습니다. 실행했던 바이트 원본은 `e2e/prototype-resilience-20260921-2052/source-prototype-resilience.mjs`에 그대로 보존하며 위 검사기 SHA는 그 원본에 해당합니다. 업무 검사 내용은 같고 수정본은 구문 검사를 통과했습니다. 해시를 포함하는 prototype 증거는 `.gitattributes`의 `-text`로 각 PC에서 바이트를 유지합니다. CRLF를 보존한 실행 증거의 diff 검사는 `cr-at-eol`을 명시합니다. 원본의 끝 빈 줄을 고의로 보존한 위 증거 사본 하나만 공백 검사에서 제외했으며 실행용 코드의 일반 공백 검사는 유지했습니다.

- 메모리 초안은 브라우저 강제 종료·기기 장애까지 복원하지 않습니다. 전체 이탈 경고와 화면 안내를 제공하며 영구 복원은 별도 설계가 필요합니다.
- 실제 AI 품질·녹음 전체 재생·영상 전체 길이·원격 저장소·최종 배포 검증은 각각 별도 게이트입니다. 이 54개 검사로 대체하지 않습니다.
- 기존 6회 통합 검사에는 근거 PATCH 후 상담 초안의 최신 내용 대조·명시 선택 단계를 추가해야 합니다. 낡은 폼에 새 revision만 붙이지 않는 계약을 유지합니다.
- 커밋·공유·TODO 인수는 메인이 수행합니다. 이 작업자는 발송·커밋·과금·공식 완료 처리를 하지 않았습니다.
