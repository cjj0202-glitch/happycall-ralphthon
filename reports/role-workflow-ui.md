# 역할별 작업대 UI 상세설계 및 검증

기준: ed2b188, pc1의 독립 구현 배정. 이 문서는 실제 브라우저 확인 완료를 뜻하지 않습니다.

## 구현 전 설계

- Before: 5개 기능 메뉴 → 모든 접수 카드 → 긴 상세 화면. 센터가 물류 근거에서 돌아오면 상담 작업대로 이동하고 저장 역할도 현재 화면에 의존했습니다.
- After: 상담원 / 센터 / 경영주 3개 역할 → 역할별 처리 큐 및 검색 → 선택 접수 상세. WMS·TMS는 해당 접수의 근거 조회이며 업무 역할과 복귀 위치를 유지합니다.
- 패턴: CRUD Split-Pane. 접수 목록은 업무 데이터 패널이며 별도 전역 내비게이션을 추가하지 않습니다. 좁은 화면에서는 높이가 제한된 목록 다음에 상세를 배치합니다.
- 기존 `tokens.css`의 의미색·간격·폰트·컨트롤·radius만 사용합니다. 새로운 팔레트·폰트·라이브러리를 추가하지 않습니다.
- 상담: 접수·확인 대기 → 센터 전달 → 완료. 센터: 새 이관 → 처리 중 → 완료. 전체보기에는 알려지지 않은 상태도 표시합니다.
- 원문 확인 → AI 정제·대조 → 근거 확인·센터 전달 순서와 다음 행동을 표시합니다. CallReview는 단계 클릭으로 분리하거나 언마운트하지 않으며 원래 재생 검증을 유지합니다.
- 센터: 이관 내용·물류 근거 → 조치 기록 → 회신 등록. 남은 조치가 있으면 중간 회신을 주 행동으로, 없으면 최종 회신을 주 행동으로 표시합니다.

## 실패·보존 조건

- 빈 큐나 검색 결과 0건에서 다른 큐의 사건을 대신 표시하지 않습니다. 검색 지우기 / 전체보기로 복구합니다.
- 선택·역할·화면 이동은 기존 접수별 세션 초안을 삭제하지 않습니다. 저장 충돌, 응답 유실, 대조 복구와 expectedRevision 계약을 유지합니다.
- 센터 물류 조회가 상담원 PATCH 권한으로 바뀌지 않으며 이관 후 접수의 근거 수정도 막습니다.
- 카카오톡·Teams 푸시는 최종 범위의 후속 기능입니다. 미구현 발송 버튼이나 성공 메시지를 만들지 않습니다.
- 검증: 실제 Home 핸들러/JSX를 실행하는 순수 하네스와 큐 정상·빈 결과·미지 상태·역할 복귀·읽기전용 반례, TypeScript 검사. 새 브라우저·서버·소켓 시작 없음. 실제 렌더 검수는 메인의 기존 화면 관측 결과를 따릅니다.

## 실행 결과

pc1 로컬, 2026-09-21 22:49 KST. 상세설계에서 정한 UI를 실제 `page.tsx`·`globals.css`·`workflow.ts`에 구현했습니다. 역할 이름은 시연 모드이며 실제 계정 인증 완료를 뜻하지 않습니다.

- `node tests/e2e/role-workflow-unit.mjs`: 61 / 61. 실제 production 모듈을 transpile하고 실제 Home JSX 버튼·검색·복귀·저장 콜백을 실행했습니다. 큐 8조합, 점포/제목/ID 검색, 빈 큐와 0검색 복구, 미지 상태 보존, 상담/센터 근거 변경 경계, 센터의 WMS/TMS 역할·복귀 위치, 실제 PATCH expectedRevision 유지가 포함됩니다.
- 변이 4 / 4 발화: 근거 잠금 제거, 센터 PATCH 역할을 view로 잘못 판정, 센터 복귀를 상담으로 변경, 전체 목록에서 미지 상태 제거.
- 기존 `node tests/e2e/save-toast-unit.mjs`: 기존 분모 48 / 48 유지, 변이 3 / 3. 새 역할·실제 workflow 의존만 추가했고 503·응답 유실·중복 동작·복구·성공 문구 검사는 줄이지 않았습니다.
- `node apps/web/node_modules/typescript/bin/tsc --noEmit --incremental false -p apps/web/tsconfig.json`: exit 0.
- 이 워커의 새 서버·브라우저·소켓·실제 네트워크 요청 0. 실제 계정 권한 검증이나 실제 사용자의 만족도 검증을 의미하지 않습니다. 메인의 기존 IAB 관측 범위는 아래에 별도로 기록합니다.

메인이 기존 IAB에서 처음 관측한 오류는 접수 목록의 flex shrink였습니다. 694px 관측 폭에서 목록 행은 height 32px / scrollHeight 99px로 내용이 겹쳤습니다. `.queue-case`의 `flex: 0 0 auto; height: auto; min-height: var(--control-xl)`와 자식 `flex-shrink: 0`를 적용했습니다.

메인의 기존 IAB tab6 수정 후 직접 관측 회신: 694px 조건에서 행 높이 113px, clientHeight 111px, scrollHeight 111px. 요청 viewport 1440 / 1024 / 390의 각 조건에서 22개 목록 행 높이는 각각 125 / 125 / 113px였고 clientHeight와 scrollHeight가 일치했으며 가로 넘침은 0이었습니다. 이는 세 폭의 DOM 치수 대조 결과입니다. 제공자 screenshot의 넓은 viewport에 검은 영역이 있어 세 폭 전체의 픽셀 시각 검증 완료로 표시하지 않습니다.

메인은 같은 기존 화면에서 센터 빈 큐 → 전체 조회 → 이관 전 접수의 편집 비활성화 → WMS 읽기 전용 → 동일 접수의 센터 업무 복귀를 직접 확인했습니다. 이 관측을 전체 시나리오·역할·저장 경계의 실제 브라우저 검증으로 확대하지 않습니다.

추가 정돈: `지금 할 일`의 카드 좌측 강조선은 dashboard-ui 규칙에 맞춰 제거했습니다. 기존 action-tint 배경, 전체 border-default 테두리, radius-md로 바꾸었으며 해당 스타일 외의 다른 작업자 변경은 보존했습니다. 이 마지막 스타일 변경 후 재렌더 관측은 메인 확인 대상입니다.

초안은 기존 `desk:<id>`, `center:<id>`, `owner:new` 저장 키를 유지합니다. 큐를 이동할 때 저장 초안을 지우지 않으며, 해당 큐에 포함되지 않는 다른 접수를 빈 큐에 자동 표시하지 않습니다. 센터에서 이관 전 접수는 열람만 할 수 있으며 조치·회신 입력도 비활성화합니다.

## 독립 검토 후 사건 선택 보존 수정

메인의 독립 검토에서 두 결함이 재현됐습니다. 기존 61개 검사에는 이 조건이 없어 통과했으므로, 앞선 통과를 해당 조건의 검증으로 소급하지 않습니다.

1. 경영주 화면에서 `handed_off`인 CASE-2를 선택하고 상담 작업대로 이동하면 `selected`는 CASE-2인데 큐는 attention으로 설정되어 상세에 CASE-0이 보였습니다. 역할 이동의 상태 분기에 handed_off가 빠진 것이 원인입니다. `queueForCase`로 상태별 큐를 정하고 명시적으로 선택한 접수를 같은 역할의 맞는 큐에서 표시합니다.
2. 센터의 CASE-2 물류 화면에서 목록을 새로고침한 결과 상태가 in_progress로 바뀌면 새 이관 큐에서 빠져 CASE-OTHER가 대신 표시됐습니다. 물류 화면은 큐 필터가 아닌 고정 selected ID로 최신 전체 목록을 조회하도록 수정했습니다. 복귀 시에도 최신 상태의 큐로 CASE-2를 유지합니다. 접수가 최신 목록에서 사라졌다면 해당 ID의 부재를 표시하고 사용자가 명시적으로 목록으로 복귀할 때까지 다른 접수로 전환하지 않습니다.

추가 검사에서는 실제 Home의 `목록 새로고침` 버튼과 request 응답 주입을 사용했습니다. 상담/센터 × WMS/TMS × 진행중/종결 조합에서 동일 ID와 최신 revision을 확인하고 복귀 큐를 대조했습니다. WMS/TMS 각각의 접수 삭제·부재·명시적 목록 복구, 경영주→상담 6개 상태 이동도 확인했습니다.

- 확장 결과: 87 / 87, 변이 7 / 7. 새 변이는 handed_off 큐 누락, 새로고침 때 물류 사건 교체, 최신 상태 대신 과거 큐로 복귀입니다.
- 최종 증거: `.local/role-workflow-unit-1789998949296/results.json`.
- 기존 토스트: 48 / 48, 변이 3 / 3. 증거 `.local/save-toast-unit-1789998949615/results.json`.
- TypeScript: `--noEmit --incremental false` exit 0.
- 이번 수정 후 새 브라우저·서버·소켓을 시작하지 않았습니다. 메인의 통합 재빌드와 최종 실제 화면 확인이 필요합니다.

## 메인 통합 확인

2026-09-21 pc1 / CJJ, 기존 실행 중인 IAB tab6 (`http://127.0.0.1:3100/`)에서 확인했습니다. 새 서버나 브라우저를 기동하지 않았으며 아래 관측은 저장·유료 분석을 실행한 전체 흐름 검사가 아닙니다.

- 최종 수정 후 센터 역할 → 전체 접수 → CASE-0001 WMS → `센터 업무로 돌아가기`를 실제 클릭했습니다. 돌아가기 버튼 1개, 복귀 후 센터 역할 `aria-pressed=true`, CASE-0001 선택 `aria-pressed=true`를 대조했습니다.
- 상담원 검색에 `없는접수-검수`를 입력해 0건·명시적인 빈 상세를 확인했고, `검색 지우기`로 19건과 CASE-0001 상세를 복구했습니다.
- 기본 폭 screenshot에서 접수 카드 겹침이 없고, 선택 문의·3단계·`지금 할 일`이 이어지는 것을 직접 확인했습니다. 마지막 전체 테두리 수정 후 왼쪽 강조선 제거도 화면에서 확인했습니다.
- 기존 tab의 수집된 warn/error 로그 조회는 0건입니다. 양성 관측은 같은 tab에서 실제 역할 버튼, CASE-0001, 검색 0→19건 및 WMS 복귀입니다. 로그 수집 이전 이력이나 모든 기능의 무오류를 주장하지 않습니다.
- 별도 화면 검수 에이전트는 자기 CUA 세션의 열린 탭 0개 때문에 실제 화면을 볼 수 없었습니다. 그 에이전트의 화면 검수 분모는 0이며 메인의 위 관측을 독립 화면 검수로 부르지 않습니다.
- PC4의 N04-U1 TMS 내부 돌아가기 문구·읽기 전용 버튼 계약은 착수 ACK 상태입니다. 메인 부모의 역할·사건 복귀 및 변경 차단과 TMS 내부 UI 인수는 구분합니다.

최종 역할 코드 수정 후 `scripts/build_deployment_bundle.py --build` 완료 시각은 22:56:33 KST입니다. 소스 전후 지문은 모두 `4b80d0c0bd8428ae576ac48b63bfa14b31c79ac116f357cf1a9478ad8b504c32`, 출력 지문은 `86b48cce78a6b16d9c026012b6cab638e27b4455ad8a7c95f7eb7977ab7fa32c`, 출력 26파일입니다. 이어 같은 스크립트의 패키징을 실행해 `dist/deployment/20260921T135653274066Z-321f4724514f`를 생성했습니다. 이는 로컬 묶음이며 실제 외부 배포 완료가 아닙니다.
