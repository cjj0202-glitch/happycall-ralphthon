# 접수 제목·원문 인용 지시 보완

2026-09-22 pc1/CJJ. 기준 `872f805`. 최근 [실제 4건 평가](evaluation/receipt-followup-20260922-review.md)의 제목 3셀·단위 1셀 실패를 보존하면서, 제목과 원문 인용의 생성 지시를 정리했습니다. 문구 변경이며 실제 모델의 개선 효과를 검증한 결과가 아닙니다.

## 관측과 변경

독립 검토자는 저장 raw를 현행 normalizer에 무과금 재처리해 4/4가 저장된 최종 결과와 같고, 실행 당시와 현재의 관련 소스 7/7이 같음을 확인했습니다. 실패 제목 3개는 raw와 최종이 같아 모델 생성 단계의 누락이었습니다. W05의 단위도 raw null을 그대로 보존한 결과입니다. 원문 인용이 존재하는 검사와 제목의 의미 충족 검사는 다릅니다.

`server/live.py`의 기존 제목·인용 문장을 대체하고 `server/analysis_schema.py`의 subject·subjectQuote 설명을 맞췄습니다. 제목만 읽어도 대상·문제·확인 요청과 명시된 불확실성을 파악할 수 있게 하고, 의미에 필요한 조건을 짧은 길이보다 우선하도록 했습니다. 확인 요청을 확인 중·완료로 바꾸지 않으며, 실제 원문에 있는 처리 단계는 보존합니다. 인용에서 점포 소개만 고르거나 부정·정정·시점·귀속 미확인을 잘라내지 않도록 정리했습니다.

기존 최초/재문의·후속 회신 구분, 문의 점포 신원과 상품 귀속의 구분, 입력을 정답으로 복사하지 않는 규칙은 유지합니다. 필드·normalizer·단위 null·원장·저장 raw는 바꾸지 않았습니다. 제목을 일괄 대체하거나 평가 ID별 보정을 넣지 않았습니다.

## 채택하지 않은 방법

EA/BOX 후보 중 support 검사를 유일하게 통과하는 값을 자동 복원하는 방식은 기각했습니다. 별도 합성 반례인 “하늘솔티백 4개와 4박스를 받았습니다.”에서 EA만 통과해 모호성을 놓쳤습니다. 이 검사는 제안 값의 근거를 제한하는 역할이며, 누락 값을 추출하는 도구로 사용할 수 없습니다. unit null과 기존 원문 우선 확인 안내를 유지합니다.

## 실행 검증

```powershell
.venv/Scripts/python.exe -B -m pytest -q -p no:cacheprovider tests/test_analysis_semantics.py::ClaimProjectionTests::test_live_analyzer_wiring_uses_claim_schema_without_fixture_answers tests/test_receipt_review_guidance.py tests/test_receipt_unit_binding.py
```

메인 실행은 exit 0, **59 passed in 2.97s**입니다. mock 전송이 실제 프롬프트·schema 구성 경로를 통과하며, 기존 null 보존·단위 귀속·원문 확인 안내 계약을 확인했습니다. 이번 저위험 문구 변경을 복제하는 새 테스트는 추가하지 않았습니다. 새 유료 호출 0회이며 이 검사 수를 모델 제목 생성 정확도로 해석하지 않습니다.

별도 읽기 전용 검토자는 실제 diff와 HEAD 대비 AST를 대조했습니다. 두 파일의 문자열 이외 AST가 동일하고 모듈 수준 함수 11개가 그대로이며, 최초/재문의·후속 회신·명시 단계·null/단위 계약의 충돌을 발견하지 못했습니다. 문구 보완 범위로 인수하며 모델 정확도 향상으로 판정하지 않습니다.

현재 실행 API 적용, 새 생산 빌드, 실제 모델 재평가는 별도이며 기존 3제목·1단위 실패는 미해결로 보존합니다.
