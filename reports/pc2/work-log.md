# N02 실행·검증 기록

실제 PC `안영일`, GitHub `MR-A83`, 등록 `pc2/ux-review`. 기준 HEAD `3c5e5a35dff7afb3a8c59bd93814bd1699d9915a`, 작업 branch `work/pc2-n02-call-review`. 사용자가 제품 보류를 해제했고 메인 #8에 18:37 KST ACK를 남겼다. 메인은 18:42 회신으로 착수를 확인했다. 이 PC의 보조 에이전트는 모두 pc2 내부 작업이며 다른 PC의 실적으로 세지 않는다.

## 상세설계와 분담

- 구현 전 `design.md`, `scenario-test-plan.md`, `integration.md`를 작성했다. 사용자 과업·UX·데이터 계약·검증의 BMAD 4관점과 미결정 처리 기준은 설계 문서에 있다.
- 담당 A는 CallReview TSX/CSS만, 담당 B는 독립 브라우저 하네스/검증 보고만, 담당 C는 음성 측정/청취 계획만 편집했다. 이 세션은 통합 계약·실측 재실행·리뷰·메인 회신을 맡았다.
- 별도 Goal을 만들거나 메인 Goal을 완료 처리하지 않았다. 실제 원본 세션 로그는 원래 로컬 위치에 보존하며 Git에 넣지 않는다.
- 기존 main 코드에서 분석 결과가 현재 상담 입력을 덮어쓸 수 있음을 확인했다. 본 모듈은 원문/AI/현재 입력을 비교하며 최초 입력의 영구 보존을 주장하지 않는다. 공용 서버·타입 계약 변경은 메인 인수 항목으로 남겼다.

## 작은 Bolt와 같은 조건 재검증

병목은 통화를 다시 들으며 수령 단위의 정정과 상담 입력 차이를 찾는 일이다. 합성 통제 입력은 주문18EA, 수령1BOX, 현재 상담 입력1EA, AI 점포ID null이다. 새 화면은 원문/전사/AI 제안/현재 입력/추가 질문을 분리하고 BOX↔EA 차이와 인용 근거를 표시한다. 실제 업무 성과 시간이나 사람 사용성 점수는 측정하지 않았다.

첫 실제 렌더에서 0.15–0.8초 구간 버튼이 정수초 절삭으로 `0:00–0:00`을 표시했다. 실제 재생은 0.8초에서 정지했지만 표시가 틀렸다. 구간 시각에 밀리초 표시를 적용한 뒤 동일 입력에서 `0:00.150–0:00.800`, 실제 currentTime0.8, 완료 callback0을 재검했다. 전체 음성 재생과 구간 재생을 혼동하지 않는 화면/동작 개선이다.

## 실측 순서와 범위

| 관측 시각(KST) | 실제 결과 | 범위/한계 |
|---|---|---|
| 18:34 | 승인 v1 백업 후 v2 WAV2 업그레이드, 자산3 SHA·크기 확인 | 새 음성 생성/유료 호출0 |
| 18:47 | PCM 비교6/6, 측정 도구 단위검사7/7 | 사람 청취·LUFS 신규 측정 아님 |
| 18:57 | 합성 경계21/21 | 실제 v2 파일은 해당 보조 실행에서 EPERM으로 미검증 |
| 18:58 | 루트 기본 Node 실행에서 기존 WAV2 읽기 정상 | ACL/보안 설정 변경·재다운로드 없음 |
| 18:59–19:01 | 실제 WAV2 포함 브라우저23/23, exit0 | Edge153, 전체 재생1배, 소스 SHA 전후 동일 |
| 19:01 | Next production build exit0, 음성 단위검사7/7 재확인 | 공용 부모 연결은 아직 안 됨 |

기존 전체 실행은 CASE1 47.15초/관측49.825초, CASE2 49.55초/관측52.910초, 각각 native ended·played범위0–끝·callback1·media error없음을 확인했다. 화면 폭1440/1024/390의 수평 넘침0, 의도한404/손상/0초와 타임스탬프 반례도 검사했다. 원문과 실제 STT의 출처 검사 assertion은 이후 더 좁은 영역으로 강화하여 별도 검증한다. 초기 실패 원문은 결과 JSON의 previousRuns에 보존한다.

## 연결 끊김과 복구

18:48–18:53 watcher에 GitHub API 연결 실패가 기록됐다. 로컬 구현·합성 검증은 계속됐다. 18:58 git fetch와 gh 사용자 조회가 성공했고, 기존 watcher PID27156은 동일 명령/생성시각으로 살아 있으며 snapshot이 다시 갱신됐다. 중복 watcher를 만들거나 정책/ACL을 변경하지 않았다. 5분 Codex heartbeat와 30초 watcher는 서로 다른 운영 장치다.

## 독립 리뷰 후 추가 반례

별도 읽기 전용 리뷰에서 구간 정지를 requestAnimationFrame에만 의존할 때 백그라운드 탭에서 지연될 수 있다는 후보가 나왔다. 완료 후 사건/음원 전환 게이트와 텍스트 접수 분기도 추가 검증 대상으로 지정했다. 재현·수정·최종 검증 결과는 `browser-validation.md`와 후속 실측 JSON을 정본으로 사용한다. 후보를 재현 전 확정 장애로 기록하지 않는다.

메인에게는 검증된 소유 파일만 branch로 공유한다. 배포·부모 통합·서비스 전체 흐름·사람 청취·실제 TEST 왕복은 각각 별도 인수/검증이며 이 보고의 PASS에 포함하지 않는다.

### 19:08 독립 반례 수정·재검

실제 보이는 브라우저에서 RAF만 중단하도록 주입한 C18은 선택 끝0.8초를 넘어1.76513초에도 paused=false로 재현됐다. trusted timeupdate는 계속 발생했다. 구간 경계 검사를 RAF와 timeupdate가 공유하도록 수정하고, 숨김 이벤트에는 선택 구간을 즉시 해제·중단했다. 같은 조건 재검에서 paused=true/currentTime0.8/callback0, 실제 played끝0.900211초를 확인했다. 이는 이벤트 간 약100ms 초과 후 정지이며 표본 단위의 정확한 컷을 주장하지 않는다. 실 브라우저의 최소화/숨김은 재현하지 못했고 명시적인 visibility 분기 주입으로 별도 검사했다.

수정 후 영향 검사10/10, typecheck와 production build exit0. 완료 후 사건/음원 전환 게이트와 텍스트 접수 재시도를 추가했다. 실제 저장 STT의 A/B 명칭과14개 구간을 유지하며 서로 다른 색과 미확인 점포를 확인했다. 신규 전사 호출은 없다. 검증 담당의 입력·기대·실측과 별도 리뷰 담당의 정적 검토를 구분하고, 최종 전체 재실행은 `validation.md`에서 확인한다.
