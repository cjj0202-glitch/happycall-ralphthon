# N02 시나리오·반례 검증 계획

기준3c5e5a3 / pc2 / 설계 단계. 아래는 실행 전 기대값이며 결과표가 아니다.

| ID | 입력·동작 | 기대 |
|---|---|---|
| C01 | 실제 CASE-0001/0002 v2 WAV 전체 재생 | 각 ended, media error0, 정상 완료 콜백. 청취 만족도는 별도 |
| C02 | 시각 있는 정정 발화 선택 | 같은 파일의 지정 구간, 끝에서 정지, 전체완료 콜백0 |
| C03 | 1EA intake / 1BOX proposal / 주문18EA | 단위 차이와 인용 표시, 주문량을 수령량으로 대체하지 않음 |
| C04 | storeId=null·quantity=null/0 | 점포·수량 미확인, 0은 그대로0, fixture 점포 자동확정0 |
| C05 | 분석 중/오류/재시도 | 상태 구분, 조건 충족 후 부모 재시도만 호출, 자체 API0 |
| C06 | sourceText·replay transcript | 대본·샘플임을 명시, 실제 STT 주장0, 원문 불변 |
| C07 | 404·음원 없음·손상 데이터 | 인라인 오류/누락, 완료 콜백0, 재시도 가능 |
| C08 | 짧은 음원·start/end 누락/음수/역전/NaN/범위초과 | 불량 구간 비활성·이유 표시, 임의 시각 생성0 |
| C09 | 전체 끝으로 seek·구간끝·이전 사건 ended | 분석 허용 콜백0, 새 사건 상태 오염0 |
| C10 | 음소거/볼륨0·배속·키보드 | 실제 audio 속성과 표시 일치, Tab/Enter/Space 조작 가능 |
| C11 | 같은ID 음원변경·사건전환·disabled 전환 | 이전 재생정지, 완료/구간 초기화, disabled 상호작용 차단 |
| C12 | 1440/1024/390px·긴전사/긴토큰·긴오류 | document scrollWidth≤clientWidth, 주요버튼/초점 보임 |
| C13 | 무음 WAV·0초/메타 오류 | 기술적 재생과 명료도 구분, 인위적 전사/청취합격0 |
| C14 | props/입력 재사용·deep freeze | 원문/analysis/intake 변경0, 전사 출처는 명시모드에만 의존 |
| C15 | 전체 브라우저 실행의 요청/렌더 예외 | 외부·제품 API 요청0, React 렌더 예외0 |
| C16 | 재생 도중 disabled 후 재개·끝으로 seek | 중단·조작 차단, seek 완료0, 새 전체 재생만 완료 |
| C17 | 선택 발화 범위 밖으로 seek | 구간 중단, 전체 완료0 |
| C18 | RAF 정지 및 탭 숨김 반례 | 구간 끝 timeupdate 보조 정지. 실제 탭숨김과 테스트 주입은 별도 기록 |
| C19 | 전체 완료한 뒤 사건/음원 변경 | 새 음원의 분석 재시도 게이트가 다시 잠김 |
| C20 | 텍스트 접수의 분석 오류·재시도 | 오디오 요소 없음, 원문/출처 유지, 음성 완료 없이 부모 재시도 가능 |

실제 컴포넌트를 별도 harness에 렌더하고 Playwright에서 사용자 동작과 브라우저 audio 이벤트/속성을 대조한다. 합성 fixture와 추가 테스트 입력은 `tests/remote/pc2/`에 두며 실제 API/유료 호출을 하지 않는다. 단순 DOM snapshot이나 함수 복제 테스트로만 수용하지 않는다. 가능한 변이 대조는 실제 핵심 보호 조건을 바꾸면 대응 검사가 실패하는지 확인하되 소스 원본을 보존한다.

실측은 `reports/pc2/validation.md`와 구조화 결과·스크린샷에 별도 기록한다. 실패→수정→같은 입력 재검을 보존하고 미실행·skip·사람 청취 범위를 공개한다. 부모 연결 검증과 전체 서비스 통합은 pc1 인수 항목이다.
