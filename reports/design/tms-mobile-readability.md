# TMS 모바일 방문도 가독성 개선

## 구현 전 상세설계 — 2026-09-21 19:15 KST, CJJ / pc1

- 문제: 독립 브라우저 검토의 390px 화면에서 760단위 SVG가 약 322px로 축소되어 계획 라벨의 실효 글꼴이 12 × 0.4236 = 5.08px였습니다. 페이지 넘침이 없다는 검사만으로 판독 가능성을 보장하지 못했습니다.
- 사용자 과업: 방문순서와 점포를 읽고 선택한 점포의 계획/실제 기록을 비교한 뒤 같은 표 행과 원본으로 이동합니다.
- 선택: TMS SVG만 760px 이상으로 유지하고 차트 내부에서 가로 이동합니다. 가상 지리·방문 순서 도식과 기존 표를 보존합니다. 선택 점포를 차트의 보이는 범위에 맞추며, 아래 한 개의 선택 요약에 방문순서·점포·계획·배송완료를 표시합니다. 요약의 버튼으로 해당 표 행에 포커스를 옮깁니다. 두 번째 표나 점포 목록을 만들지 않습니다.
- 기존 디자인 브리프 `reports/design/ui-reference-brief.md`의 표+선택 상세 패턴과 현재 semantic/scale 토큰을 사용합니다. 새 팔레트·폰트·UI 킷은 추가하지 않습니다.
- 계약 경계: API·케이스·근거·시간 가드를 바꾸지 않습니다. 메인의 미래 계획시각 표시 수정 3곳을 유지하고 실제·모바일 시각의 asOf 필터는 그대로 사용합니다. 가상 좌표/GPS 아님 표시, 정렬, Enter/Space 선택, 원본 행 펼침을 보존합니다.
- 검증 계획: 기존 실제 TSX SSR 시간검사 6개와 typecheck, Playwright 375/390/768/1365px에서 실효 계획 라벨 ≥12px, 선택 점포 가시성, 차트 내부 가로 이동, 페이지 넘침 0, 지도↔표 선택 일치, 키보드와 포커스, 정렬, 콘솔을 확인합니다. 기존 GET에 합성 fixture를 공급하고 변경 API는 차단합니다. 실제 서버 저장·유료 AI 호출·사람 사용성 평가를 하지 않습니다.

## 구현과 실제 검수

`LogisticsView.tsx`와 `LogisticsView.module.css`에 위 설계를 반영했습니다. 선택한 지도 노드의 가로 위치를 ResizeObserver로 맞추고, 표 선택과 같은 `selectedStop` 상태를 사용합니다. 선택 요약에서 표의 해당 점포 버튼으로 포커스를 이동합니다. 지도 점포의 투명한 클릭 영역은 직경 48 SVG 단위입니다. 키보드 Enter/Space 동작과 전체 표의 정렬을 유지했습니다.

첫 픽셀 검수에서 기존 트럭이 초기 1번 방문번호 위에 그려지는 점도 확인했습니다. 트럭을 방문 노드보다 먼저 그려 방문번호를 전경에 유지했습니다. 경로·속도·재생 상태는 바꾸지 않았습니다.

검수 대상: 로컬 `http://127.0.0.1:3100`, 실제 Chromium, 실제 개발 컴포넌트. GET `/api/cases`만 독립 합성 CASE-0001/0002로 응답하고 다른 API는 차단했습니다. 기존 3100/8100 프로세스를 재시작하지 않았습니다. 두 사례 × 네 폭, 총 8개 렌더를 2026-09-21 19:19~19:20 KST에 검수했습니다.

| 뷰포트 | 차트 보이는 폭 / 전체 폭 | 계획 라벨 실효 글꼴 | 페이지 가로 넘침 | 선택 점포 가시성 |
|---|---:|---:|---:|---|
| 375px | 307 / 760px | 12.00px | 0px | 2개 사례 모두 통과 |
| 390px | 322 / 760px | 12.00px | 0px | 2개 사례 모두 통과 |
| 768px | 700 / 760px | 12.00px | 0px | 2개 사례 모두 통과 |
| 1365px | 1225 / 1225px | 19.34px | 0px | 2개 사례 모두 통과 |

- 지도 점포 선택 → 선택 요약 갱신 → 같은 표 행 포커스: **24/24회**. 선택 후 지도 노드도 보이는 범위에 위치하며, 키보드 이동 시 포커스는 **2px solid**입니다.
- 표에서 선택 → 지도 `aria-pressed` 일치: **8/8회**. 계획 도착 오름차순/내림차순 정렬과 실제 행 순서 **8/8회** 대조했습니다.
- CASE-0001 390px에서 실제 Tab/Shift+Tab 순서와 Enter/Space 선택을 추가 검증했습니다. 동작 줄이기를 해제한 뒤 재생에서 트럭 좌표 변화, 일시정지 250ms 동안 좌표 유지, 처음으로에서 초기 좌표 일치를 확인했습니다. 기본 동작 줄이기 상태에서는 설명 재생 버튼이 비활성인 것도 화면에서 확인했습니다.
- 모바일 표 자체 스크롤은 375px에서 321px, 390px에서 306px까지 이동했습니다. 페이지 루트와 본문 폭은 모든 렌더에서 뷰포트와 같습니다.
- 계획 라벨 측정은 computed font-size × SVG `getScreenCTM().a`로 했습니다. CSS 최소폭을 제거한 390px 변이에서 **5.0837px**로 내려가 판독 기준이 실패하는 것을 두 사례에서 확인했습니다. 처음 동일 프레임 안에 CTM을 읽으면 이전 배율이 남아 변이를 놓쳤고, 두 animation frame 이후 측정하도록 검사기를 고쳤습니다. 단순히 CSS 선언만 검사하지 않았습니다.
- 실제 스크린샷으로 375/390/768/1365px를 열어 점포·방문번호·계획 라벨·선택 요약이 읽히는지 확인했습니다. AI 시각 검수이며 사람 사용자 만족도나 접근성 전체 인증이 아닙니다.
- 8개 렌더의 pageerror/console error **0건**, 허용되지 않은 API 요청 **0건**. 각 렌더에서 합성 사례 GET **1건**이 실제 수행됐습니다.

## 재현 명령과 증거

작업 경로는 `C:/00.프로젝트/happycall-ralphthon`입니다. Node 절대경로는 `C:/Users/choi8/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/bin/node.exe`입니다.

```powershell
# apps/web에서 실행: 종료코드 0
node node_modules/typescript/bin/tsc --noEmit --incremental false

# 저장소 루트: 실제 컴포넌트 SSR 6/6, 실제시각 가드 변이 2건 검출
node tests/e2e/tms-time-semantics.cjs

# 로컬 검수 스크립트: 실제 화면 + 읽기 전용 합성 API fixture
node .local/tms-mobile-readability.mjs
$env:TMS_CASE_INDEX='1'
node .local/tms-mobile-readability.mjs
```

- 결과/스크린샷: `.local/tms-mobile-readability/CASE-0001/`, `.local/tms-mobile-readability/CASE-0002/`의 `results.json`, `tms-{375,390,768,1365}.png`. 임시 검수 스크립트와 이미지는 로컬 증거이며 이 보고서만 Git 공유 대상입니다.
- 검수 TSX SHA-256: `0e38571d3f3b6e2cadf1dfe79c508c0a71f91b4d6897b899a9e68d6935dd0a19`.
- 검수 CSS SHA-256: `3faa8ad69e31e89a1da04881ee6367d3118c0b342d6e8b4b300965383b52069b`.

## 한계와 인수 조건

좁은 화면에서 전체 지도를 한 번에 보는 대신 내부 가로 이동이 필요합니다. 이를 안내 문구·선택 점포 자동 위치·한 개의 선택 요약·표 행 이동으로 보완했습니다. 시험 데이터의 점포명 6개보다 매우 긴 이름이나 조밀한 수십 점포의 지도 충돌은 이번 검수 범위가 아닙니다. WMS 공정도 가독성과 제품 전체의 작은 글꼴은 변경하지 않았습니다. 사람 사용성·실제 WMS/TMS·유료 AI·배포 환경 검증을 대신하지 않으며, 메인의 통합 및 독립 검수 후 인수합니다.

## 메인 인수용 독립 검수

19:21:59 KST, 구현 담당과 다른 `/root/tms_plan_ui_review`가 Chromium 151/3100에서 CASE-0001을 독립 확인했습니다. 375/390/768/1365px × 3점포=12회 모두 선택 노드 전체가 지도 뷰포트 안에 보이고 요약·표 선택이 일치했습니다. 글자 실효 크기는 12/12/12/19.34px, 본문 가로 넘침은 0px입니다. 지도 Tab/Enter/Space와 표 행 보기의 포커스 이동(2px), 내부 휠250px/ArrowRight, 재생 중 트럭·방문번호 분리, 계획 양방향 정렬(null 마지막)을 확인했습니다.

미래 계획07:01은 지도·요약·표·원본에 남고 미래실적07:02/03/04는 미등록·원본null·완료0/3이었습니다. null/잘못된 계획/기준시각 동일/잘못된asOf 추가4종도 기대와 일치했습니다. console error/warning/pageerror 각각0, 범위 내 P1/P2 0건입니다. 위 TSX/CSS SHA256과 시작·종료 값이 같았습니다. GET fixture 5회 대체·실제 API 변경/외부/유료0건이며 CASE-0002는 구현자 검수 범위에만 포함됩니다. 실제 배포·저장·사람 검증은 별도입니다.
