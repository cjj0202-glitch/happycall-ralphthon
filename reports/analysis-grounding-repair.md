# AI 사실관계 오류 4건의 서버 귀속 검사 수정

2026-09-21 20:42 KST · pc1 CJJ · `/root/grounding_four_repairs` · 기준 `bfc8543`.

저장된 실제 모델 원응답 32건을 새 정규화 경로로 처리했을 때 **문제 4건만 변경되고 나머지 28건은 완전히 동일**했습니다. 고정 입력·원응답·기존 최종 결과·원장을 수정하지 않았으며 원본 36파일 해시를 보존했습니다. 새 모델 호출은 0건입니다. 이것은 새 라이브 평가 또는 전체 안전 게이트 통과를 뜻하지 않습니다.

## 상세설계와 변경

[설계](../planning/analysis-grounding-repair.md)에 사용자 결과, 공개 계약 유지, 소유 경로, 사전 수용 기준을 먼저 기록했습니다. `server/live.py`의 정규화에서 새 순수 모듈 `server/claim_grounding.py`를 호출합니다.

| 결함 | 원인 | 수정 후 실제 저장 응답 처리 결과 |
|---|---|---|
| FIX-M07 | 긍정 주문 뒤 같은 문장의 수령량 ‘못 셈’까지 주문 부정으로 검사 | 명시 주문 10 EA가 summary에 복구됨. 긍정 주문을 찾지 못했다는 질문이 제거됨. 수령 미확인은 유지 |
| FIX-W04 | 주문 인용에 없는 숫자·단위를 모델 제안 그대로 신뢰 | 주문 상품은 유지, 주문 수량·단위만 미확인. 수령 6 EA는 그대로 유지 |
| FIX-W05 | 수령 필드와 별개로 만든 unknowns를 그대로 출력 | 수령 4 EA 진술 존재와 실제 인도·기록 일치의 별도 확인을 구분. 다른 점포 귀속 미확인은 유지 |
| EDGE-12 | 오늘 인용에 없는 BOX를 지난주 인용에서 전용 | 오늘 quantity와 unit은 모두 null. 지난주 원문은 변경하지 않음 |

검사는 인용을 주문/수령 연결절과 명시된 현재/과거 대비로 나누고, 제안된 값이 해당 절의 수량·단위 토큰으로 지지되는지 각각 확인합니다. 없는 값을 추출하여 채우거나 포장입수로 환산하지 않습니다. 수령 0, 단위만 알려진 경우, 단위 없는 명시 수량, 고유어·한자어 수사를 별도 대조했습니다. 같은 숫자가 이웃 상품·주문/수령 양쪽에 있어도 다른 단위를 가져오지 않게 검사했습니다.

## 재현과 실측

저장소 루트에서 실행했습니다. 모든 테스트는 mock/로컬이며 API 비용을 발생시키지 않았습니다.

```powershell
.venv/Scripts/python.exe -X utf8 -m unittest tests.test_analysis_grounding_regression -v
.venv/Scripts/python.exe -B -X utf8 -m pytest -q tests/test_analysis_grounding_regression.py tests/test_analysis_repair.py tests/test_analysis_semantics.py -rs
```

| 검사 | 기대 | 실제 |
|---|---|---|
| 새 합성 최초 10테스트, 수정 전 | 4결함과 관련 경계를 실제로 실패시킴 | 8 실패 / 2 통과 |
| 최초 수정 후 관련 3모듈 | 기존 의미·인용 보호와 새 회귀 유지 | unittest 67/67 통과 |
| 추가 반례 | 주문 주장의 철회·인용을 계속 차단 | ‘사실이 아닙니다’ 경계 1건 실패 발견 후 주문 철회 형태 검사 보완 |
| 최종 분석 3모듈 | 양성·음성·시간·단위·0·한국어 숫자·원문 불변성 유지 | **74 passed / 114 subtests passed / 4.94초** |
| 변이 4개 | 검사를 없애거나 모든 값을 지우면 회귀가 실패 | 값검사 우회, 과도한 null, 주문절 분리 제거, unknown 정합성 제거 모두 검출 |
| 기존 코드의 저장 결과 재현 | 입력과 원응답을 같은 기존 코드로 처리 시 일치 | **32/32 기존 최종 analysis와 완전 일치** |
| 새 코드의 저장 응답 재처리 | 4결함 수정·그 밖의 변화 검토 | 변경 4 / 무변경 28, 스키마 32/32 유효 |
| 자동 4필드 대조 | storeId·quantity·unit·departmentId에서 새 실패가 없어야 함 | **128/128 일치**. 기존 EDGE-12 unit 한 셀이 null로 정정됨 |
| 원본 불변성 | dataset/manifest/events/summary와 32결과 보존 | **36/36 파일 SHA-256 일치** |

subject/request와 department/replyDraft/facts는 32건 모두 이전 값 그대로입니다. 기존 의미 오류를 해결했다고 보고하지 않습니다. 안전 4건 수정에 대한 독립 재검토는 메인의 인수 과정입니다.

넓은 서버 검사도 실행했으나 병렬 작업 중인 다른 소유 경로의 변경에 의해 실패한 이력을 보존합니다.

- `unittest discover -s tests -p 'test_*.py'`: 184 실행, 2 errors, 1 skip. `server/openapi.yaml`의 인라인 description 쉼표가 잘못된 mapping 키로 파싱되어 ASGI 시작 검사 2건 실패. 메인에게 전달했고 메인이 해당 문서를 수정했습니다.
- 분석·backend·runtime config/storage·평가·배포 8모듈 pytest: **380 passed, 4 failed, 3 skipped, 138 subtests passed / 13초**. 실패 4건은 `test_runtime_storage.py`의 실제 Blob 오류 어댑터 경로가 context 없는 내부 호출을 사용하는데 병렬 변경 `handlers.create_intake`가 새로 `request.headers`를 접근하면서 발생했습니다. 메인에게 전달했고 이 워커는 소유 밖의 파일을 고치거나 검사를 삭제하지 않았습니다.
- skip 3건은 Windows symlink 권한 미보유에 따른 기존 실제 링크 검사입니다. 실행 성공으로 세지 않습니다. 외부 변경 통합 후 전체 서버 재검사는 메인이 수행합니다.

## 재처리 증거

로컬 전용 결과: `.local/grounding-repair/reprojected-32-final.json`.

- 파일 SHA-256: `37dd126db2daa8240955d99b8a9a0060b01b65f3308e0bef95d76809cfe97602`.
- source run: `.local/evaluation/20260921T112012Z-62da8e1711`.
- 새 `server/live.py` SHA-256: `51cbce2c274436803d9ecca5ea6adaa6684073605d64cac06ff04b53b0dd6e51`.
- `server/claim_grounding.py` SHA-256: `b916beb53cc2f609d632f9a259750ed566d862695f9e26bd22391e74244041e8`.
- 회귀 테스트 SHA-256: `3eb0c493dd0e419b02d258b84f84fec934216d5c166d1552373a65e29b7da427`.

로컬 증거에는 원본 36파일 해시, 위 코드 해시, 각 32건의 이전/새 결과와 변경 필드가 있습니다. 외부 Git에는 이 보고서와 새로 작성한 합성 회귀만 올리며 원응답 사본을 올리지 않습니다. 기준 재현은 `git show bfc8543:server/live.py`를 별도 메모리 모듈에 로드하여 `normalize_analysis`를 호출했고 현재 함수와 대조했습니다.

## 한계와 인수

이 모듈은 한국어 의미를 완전히 판정하는 엔진이 아닙니다. 표면 인용과 제한된 연결절·수사·단위의 필요조건 검사입니다. 화자 신원, 실제 배송, 모든 생략·정정·복수 상품, 임의 단위·특수 수사 표현을 인증하지 않습니다. 사람이 원문과 대조해야 하며 값이 자동 채워지지 않았다는 이유만으로 실제로 값이 없다고 단정해서는 안 됩니다.

미확인 문장 조정은 **같은 현재 상품의 단순 수령 수량/단위 항목이며 두 값의 인용 지지가 있을 때**만 적용합니다. 복합 조건·다른 시점·다른 상품·라벨 귀속은 보존합니다. 복잡한 자유문 모순을 모두 없앴다는 주장은 하지 않습니다.

새 유료 평가·라이브 STT·원격 PC 인수·배포·커밋·편지 발송은 이 워커가 수행하지 않았습니다. 새 서버 모듈의 배포 포함은 메인이 `build_deployment_bundle.py` whitelist에서 처리합니다. 메인은 변경 파일과 새 회귀를 독립 검토한 뒤 잔여 예산 안의 라이브 canary 여부를 판단합니다.

## 20:49 독립 인수 보류 후 후속 수정

[독립 검토](analysis-grounding-independent.md)의 G1~G5 다섯 반례를 원문·기대값 그대로 새 테스트에 고정했습니다. 해당 보고서와 원래 `.local/grounding-independent/independent-results.json`은 수정하지 않았습니다. G3는 이 수정에서 새로 만든 P1 회귀였음을 인정하며, 앞선 74개 테스트 통과만으로 인수할 수 없었습니다.

먼저 새 고정 반례와 추가 변형을 이전 구현에 실행하여 **7 failed / 4 passed / 8 subtests passed**를 기록했습니다. 독립 원래 5종으로는 기존과 동일하게 G1~G4 실패, G5 통과였습니다. 하나의 G3에 옳은 단위와 틀린 단위 제안 두 번을 실행하므로 테스트 도구의 실패 개수와 독립 반례 종류 수는 다릅니다.

후속 수정은 숫자를 선택하기 전에 문장의 역할·극성/시점·대상 연결을 좁힙니다.

- 관형 주문의 수량 뒤 `중/가운데 실제 수령`과 명시 수령절을 나눕니다. G3의 주문 7 BOX를 수령 범위에서 제외하면서 실제 7 EA를 보존했습니다.
- 수령 부정, 미확인 접속절, 미래 수령 예정절을 뒤의 다른 상품 긍정 수령과 분리했습니다. 그 절의 부정·미래 수량은 실수령 필드에 쓰지 않습니다.
- 각 절의 명시 상품 또는 바로 앞 한 상품을 받는 명백한 생략 표현만 인정합니다. 이웃 상품의 단일 수량도 대상에 전용하지 않으며 비슷한 상품 이름의 부분 문자열은 동일 상품 근거로 쓰지 않습니다.
- 모든 수량을 지우지 않음을 실제 양성으로 확인했습니다. 새 변형 다섯 문장은 단위·어미·상품명을 달리하여 옳은 수령값 보존과 잘못된 다른 단위 차단을 쌍으로 실행했습니다. 다른 상품명·`말고` 대비 3개도 추가했습니다.

| 최종 검사 | 실측 |
|---|---|
| 독립 G1~G5 동일 스크립트 재실행 | **5/5 통과**. G3 올바른 7 EA 보존, 잘못된 BOX만 null |
| 분석 3모듈 | **81 passed / 124 subtests passed / 5.22초** |
| 분석·backend·runtime·평가·배포 8모듈 | **392 passed / 3 skipped / 148 subtests passed / 11.14초** |
| 저장 원응답 32건 다시 정규화 | 앞선 1차 수정 결과와 **32/32 완전 일치**, 기존 원본 대비 4건 변경 유지 |
| 자동 4필드 | **128/128 일치**, 새로운 자동 필드 회귀 없음 |
| 원래 평가 run | **36/36 파일 해시 유지** |
| 기존 독립검토 결과 | SHA-256 `6371565f20a09e23bcf921892a0678d3d4fe399c36652a39c94c478386fdb335` 유지 |

위 넓은 테스트는 메인의 handler 호환 수정이 반영된 뒤 재실행한 결과입니다. 앞선 병렬 변경 시 발생한 실패 기록은 삭제하지 않았습니다. 3 skip은 동일한 Windows symlink 권한 제약입니다.

독립 스크립트는 내용을 메모리에 읽어 **출력 폴더만** `.local/grounding-repair/independent-probe-v2-final`로 변경해 실행했습니다. 최초 재실행 래퍼는 같은 Python 전역 이름을 써 마지막 원본 해시 비교에서 `AttributeError`가 났습니다. 저장된 5종 결과와 원본 보존을 별도로 확인한 뒤, 별도 실행 네임스페이스로 전체를 재실행하여 종료 코드 0과 모든 assertion을 확인했습니다. 검사 기대를 바꾸지 않았으며 기존 스크립트·결과를 덮어쓰지 않았습니다.

최종 로컬 증거: `.local/grounding-repair/independent-probe-v2-final/independent-results.json`.

- 결과 SHA-256: `de8eab048e207d43a77baace5aaa6016478d7ad0bc9b12056cd797f0eb55dc33`.
- `server/live.py`: `51cbce2c274436803d9ecca5ea6adaa6684073605d64cac06ff04b53b0dd6e51`.
- `server/claim_grounding.py`: `c2503010ce4e4ae3523057a58fe7f724aafd05061a25ef256b3f19163fae83c9`.
- `tests/test_analysis_grounding_regression.py`: `040b40ca4e1e1ebacbc7ca78646f9277c872be27149f458cd1b91cb5a7a61a4f`.

이번 결과도 언어 전체의 의미 추론을 인증하지 않습니다. 원문 제안의 자동 지지 검사이며, 모호한 생략·새 문법·상품명 변형·복합 정정은 원문 대조가 필요합니다. 고정 반례를 통과한 사실과 다시 독립 인수된 사실을 구분하며 메인의 재검토를 기다립니다. 이 후속 단위에서도 API·커밋·발송은 0건입니다.

## 명사형 부정·예정 2건의 추가 수정

[독립 재검](analysis-grounding-independent-recheck.md)의 양성/음성 두 쌍을 그대로 테스트에 추가했습니다. `받은 것이 아닙니다`와 `수령 예정입니다`를 실제 수령으로 채택한 잔존 결함을 인정합니다. 앞선 독립 보고서·결과·원본 데이터는 보존했습니다.

명사화된 수령 사실에 붙는 `것/건/사실/상태 + 아니다/없다`, 수령·도착의 `예정/계획`을 해당 수령절의 제외 조건에 추가했습니다. 검사 대상은 명사형 부정 또는 미래 예정이며, 양성인 `수령한 것이 맞습니다`와 이미 실행된 `수령 예정대로 받았습니다`는 유지합니다. 예정이라는 단어가 포함됐다는 이유만으로 모든 수령을 지우지 않습니다.

| 검사 | 실측 |
|---|---|
| 수정 전 새 2테스트(하위사례 포함) | 음성 6개 실패, 양성 4개 보존. pytest `6 failed, 2 passed, 4 subtests passed` |
| 수정 후 분석 3모듈 | **83 passed / 134 subtests passed / 6.73초** |
| 독립 두 쌍 동일 입력 | 양성 2/2 보존, 음성 2/2 수량·단위 null |
| 기존 G1~G5 | **5/5 통과** |
| 저장 32응답 | 이전 수정 결과와 **32/32 동일**, 자동 4필드 **128/128**, 원본 **36/36 해시 보존** |
| 기존 독립 증거 | 스크립트·두 쌍 결과·재검 보고서 실행 전후 SHA-256 일치 |

기존 재검 래퍼의 출력 경로만 메모리에서 `.local/grounding-repair/nominal-v3`로 변경해 별도 네임스페이스에서 실행했습니다. 기존 파일을 덮어쓰거나 기대값을 바꾸지 않았습니다. 라이브 API 호출 없이 결과를 재처리했습니다.

- 새 두 쌍 결과: `.local/grounding-repair/nominal-v3/additional-pairs.json`, SHA-256 `09b7dc135a9b5696ea2ff3a2895c11cb299cc3807405551db63e690625470e58`.
- G1~G5 및 32건 결과: `.local/grounding-repair/nominal-v3/independent-results.json`, SHA-256 `7ea9184c0a0dd28e0917789fd528d80af97e0ce31ee4d42d21920180604973cb`.
- `server/claim_grounding.py` SHA-256 `c98947e06f567560c2d89eb3f75561a9d6bf78e98e42bf774784f60e42b3243e`.
- `tests/test_analysis_grounding_regression.py` SHA-256 `a5ff6081e1088fdbad7e8abe5db4efbe7595b26352bfb28f96daa3afcef342ae`.
- `server/live.py`는 이 단위에서 변경하지 않았으며 SHA-256 `51cbce2c274436803d9ecca5ea6adaa6684073605d64cac06ff04b53b0dd6e51` 그대로입니다.

메인의 통합 6회 검증을 위해 제품·테스트 코드를 동결했습니다. 이번 한정 수정은 위 지원 문형의 필요조건 방어입니다. 언어 전체의 부정·미래·복합 인용을 인증하거나 모든 수령의 안전성을 증명한 것이 아닙니다. 새 라이브 모델 성능·실제 STT·전체 업무 안전은 별도 인수 대상으로 남습니다. API·커밋·발송은 수행하지 않았습니다.
