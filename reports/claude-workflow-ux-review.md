# Claude Code 적대 검토와 역할 UX v2 인수

2026-09-21 23:20~23:44 KST · pc1/CJJ · 기준 HEAD `96fec8d`

상담원의 원문 확인·접수 편집·근거 조회·전달과 센터의 조사·조치·최종 회신을 실제 이동 가능한 작업 순서로 정리했습니다. 기존 IAB 1201×791에서 화면·음성 연속 재생·읽기 전용 상태를 검증했습니다. 모바일 렌더, 새 전체 저장 흐름, 배포는 이번 인수에 포함하지 않습니다.

## 실제 Claude Code 대화

- 실제 설치된 Claude Code 2.1.278 CLI를 실행했습니다. Codex 서브에이전트를 Claude로 표현하지 않았습니다.
- 고정한 프론트엔드 소스 6개와 `design-brief`, `redesign-existing-projects` 및 기존 디자인 토큰/CRUD Split-Pane 원칙을 입력했습니다. 내장 도구/MCP를 비활성화한 읽기 전용 텍스트 검토이며, Claude가 브라우저를 직접 본 것은 아닙니다.
- 동일 세션 `b3648c8f-6dde-4031-a61e-5fc0eb3b26ea`에서 최초 검토와 메인의 반박·수정안에 대한 후속 검토를 실행했습니다. 두 결과 모두 `terminal_reason=completed`, `is_error=false`, permission_denials 0. 각각 187,535ms / 107,916ms입니다.
- 고정 입력 해시, 실행 입력과 결과는 `.local/claude-ux-review/input-manifest.json`, `result.json`, `followup-result.json`에 보존했습니다. 원본 CLI 결과는 공유 Git에 넣지 않습니다. 최초 답변의 Write 코드블록은 제안 텍스트이며 실제 파일 쓰기 실행이 아닙니다.

## 지적을 그대로 믿지 않고 판정한 내용

| 지적·제안 | 판정과 적용 |
|---|---|
| 반복 헤더·제목·안내가 업무를 화면 아래로 밀어냄 | 기존 화면 측정 후 브랜드/역할을 한 줄, 동기화/초안을 한 유틸 행으로 압축 |
| 원문이 기본 접힘, 비교 결과와 입력란이 떨어짐 | 음성 대화록/웹 원문 기본 펼침. 입력란 바로 아래 AI 제안 5개 배치 |
| 전체 대조표를 폼 위에 추가 | 다시 입력을 밀어내므로 기각. 상세 대조 기능은 보존하고 핵심 차이만 입력 옆에 노출 |
| 같은 값에 동일/완료 배지 | AI와 입력이 동시에 틀릴 수 있어 새 필드에는 동일 성공 표시를 넣지 않음 |
| 수량 대조 맥락이 접힌 상세에만 남음 | 주문18EA/수령1BOX 같은 참고값과 자동 환산 금지를 입력 옆에 공개 |
| 최하단 패널에 sticky bottom만 지정 | 최초 화면에서 보이지 않는 문제를 해결하지 못하므로 기각. 고정된 단계 링크와 명시적 앵커로 이동 |
| 웹 원문을 음성 제작 대본처럼 접음 | 음성 제작 대본만 별도 접기. 웹 원문은 직접 펼침 |
| 차단 원인이 버튼에서 불명확 | 전달·회신 행동 옆에 현재 조건 표시. 이미 전달/완료한 사건은 실패로 표현하지 않음 |

Claude의 첫 답변에 있던 '첫 과업 0픽셀', details 중첩, 사건 전환 시 상단 자동 복귀 주장은 실제 확인 범위를 넘었습니다. 메인이 반박했고 후속 답변에서 정정됐습니다. 사람 사용성 동의를 만들어 기록하지 않았습니다.

## 구현 경계

설계: `planning/role-workflow-ux-v2.md`. 메인 소유 page.tsx/globals.css, 로컬 워커 소유 CallReview.tsx/module.css로 나눴습니다. 소유 동결 후 메인이 `.draft-notice`에 전역 details 스타일이 붙어 유틸 행이 96px가 되는 문제를 실제 화면에서 발견해 margin/padding/border를 정리했고 50px로 재측정했습니다.

새 색·폰트·UI 라이브러리를 추가하지 않았습니다. 원격 PC의 TMS/미디어 전달/Blender 소스, API, 저장 계약은 변경하지 않았습니다. 카카오톡·Teams 실제 발송은 최종 연동 대기입니다.

## 같은 화면에서의 실측

기존 IAB `http://127.0.0.1:3100/`, 실제 viewport 1201×791, CASE-0001, 문서 상단 기준입니다.

| 측정 대상 | 변경 전 | 변경 후 |
|---|---:|---:|
| 헤더 높이 | 118px | 58px |
| 별도 페이지 제목 | 84.3px | 시각 중복 제거, h1 접근성 유지 |
| 동기화/초안 행 | 97.5px | 50px |
| 사건 제목 시작 | y406 | y140 |
| 통화 다시 듣기 제목 | y715 | y383 |
| 첫 점포 입력 시작 | 이전 값 미측정 | y507.5, 최초 화면 안 |
| 센터 이관 정보 폭 | 334.26px | 802.5px |

- 고정 단계 탐색 높이54px. 접수 편집 링크1회 후 대상 y96·실제 포커스 `intake-editor`; 물류 근거 y95.80·포커스 `desk-evidence`. 왕복 후 CASE-0001·입력 SYN-ST01 유지.
- 전달 단계 링크1회 후 대상 y445.14로 화면 안 도달. 버튼 비활성 사유가 편집/확인 체크 2개로 표시됩니다. 앵커 이동 자체는 저장하지 않습니다.
- CASE-0002 수량1/단위BOX, 주문비스킷18EA와 수령휴지1BOX 표시. AI 제안5개 확인. 값이 같은 것을 검증 완료 배지로 표시하지 않습니다.
- CASE-0002 전체 통화2배속 재생 중 물류 근거로 이동: currentTime 0→5.973초, paused=false. 이후 ended=true/currentTime49.75=duration49.75, 분석 버튼 disabled=false. 새 AI 분석 호출은 하지 않았습니다. 이것은 재생 연속성 증거이며 한국어 자연스러움 합격이 아닙니다.
- 웹 INT-B8DDEF12 원문이 기본 펼쳐져 실제 입력 문자열을 표시합니다. 음성 제작 원본으로 잘못 라벨링하지 않습니다.
- 센터 미이관 INT-B8DDEF12의 조치/회신 disabled 유지, '상담원이 확인 후 센터로 전달해야 회신할 수 있습니다' 표시. 조치/회신 링크 착지 y180.59/y288.59, 제목과 필드가 고정 탐색에 가려지지 않았습니다.
- 완료 INT-5472B4DC는 '최종 회신이 등록되어 처리 완료된 접수입니다'로 표시하고 중간/최종 회신 모두 disabled입니다.
- 경영주 역할 왕복 뒤 기존 미제출 문의 초안 본문이 유지됐습니다. 목록22건 그대로, 새 문의 생성/저장/이관/회신0회. 미저장 초안 설명은 다른 역할/접수의 초안 포함임을 명시합니다.
- 관측한 화면의 가로 넘침0. 마지막 콘솔 error 조회0건. 이전 API CORS 실패가 해결됐다는 뜻은 아닙니다.

1024×768·390×844 viewport 설정 도구는 호출됐지만 DOM innerWidth/innerHeight가 계속1201×791이었습니다. 따라서 새 버전의 3폭 렌더는 **NOT_VERIFIED**입니다. 임시 설정을 reset했고 새 브라우저나 서버를 만들지 않았습니다. CSS 컨테이너720px/좁은폭 규칙의 존재를 모바일 실제 통과로 바꾸지 않습니다.

## 영향 검사와 독립 검증

| 검사 | 실제 결과 |
|---|---|
| TypeScript `tsc --noEmit --incremental false` | 최종 exit0 |
| 역할 검사 `node tests/e2e/role-workflow-unit.mjs` | 87/87, 변이7/7 검출 |
| 저장 알림 `node tests/e2e/save-toast-unit.mjs` | 48/48, 변이3/3 검출 |
| CallReview 워커의 React SSR/정적 대조 | 33/33, 별도 보고서 참조 |
| 독립 HEAD AST 대조 | Home28/Desk32/Center15 기존 선언 동일, ReviewSession46개 중 sourceContent만 변경 |
| 독립 실제 조건식 조합 검사 | transferBlockers 2,048조합/canTransfer 불일치0; 회신512조합 중 차단 상태에서 완료가능 안내0 |
| 독립 음성 속성/이벤트/key 검사 | audio20/20 동일, CallReview 이벤트·disabled·key34/34 동일 |
| 앵커 ID/대상 | 7개 각각1개 |
| `git diff --check` | exit0, CRLF 변환 안내만 있음 |

최초 역할 검사는 nav.children[0]의 배열 모양을 고정한 검사 때문에 실패했습니다. 실제 `aria-label=업무 역할` nav에서 하위 버튼을 세는 검사로 바꿨으며 역할 버튼3개 조건과 기존 변이 검출은 유지했습니다. 기능 실패를 기준 완화로 숨기지 않았습니다.

## 남은 항목

- 새 버전의 모바일/태블릿 실렌더와 실제 상담 담당자 사용성 관찰.
- 최신 생산 빌드/배포 묶음 생성. 이전23:09 빌드를 이번 v2의 빌드로 인용하지 않습니다.
- 실행 중 API의 오래된 CORS 설정 때문에 신규 웹접수 전체흐름0/2였던 차단은 별도 미해결입니다. API 재시작에 대한 기존 자동 승인 검토 거부를 우회하지 않았습니다.
- 국내 더빙 최종 음원, 전체1080p CCTV 완주/제품등록, 영속 저장 및 실제 최종 배포.

인수 범위는 위 역할 UX 코드·영향 검사·기존 데스크톱 화면 관측입니다. 전체 솔루션·사람 검수·배포 완료를 뜻하지 않습니다.
