# 새 배포 묶음 독립 파일·ASGI 인수

2026-09-21 22:04:23 KST / pc1 CJJ / 읽기 전용 독립 검토. 대상은 `dist/deployment/20260921T130059090033Z-181aa4070109`입니다. **114/114 관측 통과**. 실제 클라우드 배포나 브라우저 검증을 뜻하지 않습니다.

## 파일 정합

| 항목 | 기대 / 실제 |
|---|---|
| manifest 파일 집합 | **56/56**, 누락0·추가0, 모든 bytes/SHA256 일치 |
| payload 합계 | **12,798,884 B**, manifest와 동일. manifest 자체를 포함하면 파일57개 |
| 원본 보존 | 검사 전후 bundle57개 전체 크기·SHA 동일. pycache 생성 없음 |
| 빌드 source fingerprint | 현재 입력32개를 별도 목록·canonical JSON 계산으로 재산출, `716e1cdc1a2b738b50f74c5cbcb62f8564e92e4d34e8b49f0b3d38790a7048be` 일치 |
| 빌드 output fingerprint | export26개 독립 재산출, `2b2889efda74036be37a2936763f05a376c4a83fb4e9f65a1134918077b571aa` 일치 |
| 원천/공개/배포 fixture | 작업 정본·작업 public·작업 export·bundle 정본·bundle export **5/5**, 각23,693 B 및 SHA `5edf80669f7def9d3e10ceea910086c41026ff1252fbc638b97641562f3d84f4` 일치 |
| 제품 커밋 관계 | `git merge-base --is-ancestor 7ca0e8b 181aa4070109b7024cd4aa3756502a55615c3844` exit0 |

메인은 이전 묶음에서 public 예시가 v2로 남아 `SYNTHETIC_FIXTURE_COPIES_DIFFER`로 거부됐다고 보고했습니다. 이 검수는 그 이전 실패를 PASS로 바꾸지 않고, 수정 후 새 묶음의 실제 다섯 사본을 대조했습니다.

## 음성·영상·자막

v3 manifest의 Release 이름은 `demo-media-20260921-audio-v3`입니다. 아래 파일을 bundle의 public/export 양쪽에서 실제 읽어 **6/6** 크기·SHA를 확인했습니다. 외부 Release 재다운로드는 이번 검수에서 실행하지 않았습니다.

| 자산 | 바이트 | SHA256 |
|---|---:|---|
| CASE-0001.wav | 2,263,278 | d4eeb6448ff8969f27034ecade9a050ee212468585c6300b19c9a0ebdc372931 |
| CASE-0002.wav | 2,388,044 | 6fe83afb8552765ab92722b838d2a0905f507551c4f98c58fa62a515033abaa0 |
| sorter-demo.mp4 | 1,097,133 | e6cad3cf9f999b596a0fef3e3d463170e0d279568a31a4be49366e5383908881 |

WAV PCM 헤더/프레임으로 길이를 다시 계산하여 CASE1=47.15초, CASE2=49.75초를 확인했습니다. 각8개 자막의 발화 시작/끝은 정렬됐고 음원 길이 안에 있습니다. CASE2 audioUrl의 `?v=` 값과 transcriptTiming.audioSha256은 실제 v3 해시와 같으며, 417000프레임 뒤 4800프레임 추가 메타데이터, 네 번째 발화17.75초, 마지막 발화끝49.40초가 연결됩니다. 단어별 STT 정렬·사람 청취 정확도를 증명하지 않습니다.

새 page JS에서 CCTV schema·객체 조사 문구·rVFC·wrapper fullscreen 코드, CSS에서 CctvInspector/fullscreen/reduced-motion을 확인했습니다. 공통 저장의 컴파일된 함수는 검증/PATCH 전에 `p("")`로 이전 성공 토스트를 비웁니다. 문자열/코드 존재 및 빌드 지문 확인이며, 화면 동작 재실행이 아닙니다.

## 소켓 없는 실제 DeploymentRouter 검사

bundle의 `server/deployment_app.py`를 직접 import했고 해당 파일 경로도 대조했습니다. `create_deployment_app(environ=명시적 합성 값, api_app=합성 ASGI 대역)`을 만들고 **httpx.ASGITransport**로 호출했습니다. 실제 API/키/저장소/환경파일은 연결하지 않았습니다.

ASGI 관련 **33/33 관측 통과**:

- 무인증 루트/fixture/버전 음원/API GET은401이며 Basic challenge가 있습니다.
- 공개 `/healthz`만 상수200이고 API 대역을 호출하지 않습니다.
- 루트/fixture/음원2/영상/실제 page JS의 GET·HEAD12건: 원본 전체 바이트/SHA·Content-Length·빈 HEAD body 일치, private/no-store 유지.
- 미디어3개 각각 first64·suffix64 Range는206 및 정확한 바이트, 파일 밖 Range는416과 `Content-Range: bytes */<실제길이>`.
- 미등록·`.env`·서버 소스·미등록 JSON은404, 정적POST는405.
- 정적 요청에서 API 대역 호출0. 인증된 `/api/health`만 명시적 대역에1회 도달하며 대역 표식을 확인했습니다. 실제 업무 API 검사는 아닙니다.

첫 검사 **111/114**는 검사기의416 헤더 기대값에서 `bytes ` 접두사를 빠뜨린 오류였습니다. 제품 응답은416과 `bytes */크기`였고, 설치된 Starlette `responses.py:373`의 실제 응답 코드를 대조한 뒤 검사기만 수정했습니다. 원본 `first-check.py`·`first-results.json`을 보존하고 같은 묶음으로 **114/114** 재실행했습니다. 제품 파일 수정0입니다. Connexion import의 StarletteDeprecationWarning 1건은 존재하며 오류0으로 숨기지 않습니다.

## 재현·한계

```powershell
.venv/Scripts/python.exe -I -B .local/bundle-review-2200/check.py
```

증거는 `.local/bundle-review-2200/results.json`, 검사기 `check.py`, 실패 원본2개입니다. 서버 listen0·브라우저 실행0·외부 네트워크0·라이브 AI0·기존 런타임 중지0이며 거부된 서버/브라우저 실행을 다른 방법으로 우회하지 않았습니다.

클라우드 설치/런타임 크기, 실제 HTTPS/브라우저, Blob 지속 저장, 실 API는 미검증입니다. 현재 `/demo/` 정적 허용 확장자는 wav/mp4뿐입니다. 향후 실제 tracks.json을 등록할 때는 승인된 JSON 경로 계약도 함께 확장해야 합니다. 현재 묶음은 tracks 미등록이므로 이를 현재 동작 실패로 판정하지 않았습니다.
